# CLAUDE.md — Stennisfy
## Agent briefing — read this fully before touching any file

---

## What this project is

Stennisfy is a tennis betting analytics SaaS dashboard for serious ATP bettors. It tracks matches across Grand Slams, ATP 1000s, 500s, 250s, Challengers, and ITF. Members use it daily to analyse matches, odds, and player data for betting decisions.

**Product brand:** **Stennisfy** — this is the name rendered throughout the UI. The design export build contains zero rendered "BSP" strings (verified). BSP Consult is the *business* behind the product. "BSP" is deliberately retained in the source folder name, archived backups, the mobile prototype, the Login-options exploration, the Funnel wordmark and the handoff docs — **do not rename those**; they are intentional, not stragglers.

**Owner:** Michael (BSP Consult, the business) — Belgian, francophone, digital nomad  
**Repo:** `michaeldk1996/SAAS`  
**Live URL:** `michaeldk1996.github.io/SAAS/`  
**App domain (future):** `bspconsult.app`  
**Reference competitor:** matchup-tennis.fr (structure reference only — never copy their data or copy)

---

## Tech stack

| Layer | Detail |
|---|---|
| Frontend | Single HTML file: `bsp-consult-dashboard.html` |
| Pipeline | `bsp-pipeline.js` (Node.js) — runs every 15 min via GitHub Actions |
| Data files | `matches.json`, `tournament-profiles.json`, `tournament-progression.json`, `player-profiles.json` |
| Odds | The Odds API (Grand Slams, 1000s, 500s) + OddsAPI + Oddsapi (ATP 250 and broader coverage) |
| Tennis data | api-tennis.com (fixtures, results, H2H, surface stats, box scores) |
| Historical | Jeff Sackmann tennis_atp + MatchChartingProject (CC BY-NC-SA — internal R&D use; commercial serving to paying members gated until a commercially-licensed alternative — see **Model R&D mode** below) |
| Automation | GitHub Actions cron `*/15 * * * *` |
| Backtest tools | `backtest_elo.py`, `backtest_demo.py`, `demo_matches.csv` (Elo research, internal only) |

---

## Current state snapshot
*As of July 2026*

### ✅ Built and working

- **Today's Matches page** — live odds, match cards, form indicators, tournament filters, player search, day tabs (Today / Tomorrow / Live & Past)
- **Match Analysis modal** — eleven tabs, in rail order: Key factors, News, Playing style, Form, H2H, Match Stats, Progression, Overview, Tournament, Weather, Odds. (The former "Extra stats" tab was removed from the build; News was added. Verified against the rendered modal, `design-export/computed-styles.json` state `modal-news`, 2026-07-31.)
- **Player Profile page** — radar chart (Player DNA), serve/return stat cards, surface performance, recent form, key insights, season win rate chart
- **Tournament Profile page** — built
- **Tournament Reports page** — built
- **Head-to-Head page** — built (known surface filter bug, see below)
- **Playing Styles page** — partially built / placeholder
- **Track Record page** — partially built / placeholder
- **Methodology page** — partially built / placeholder
- **GitHub Actions pipeline** — running every 15 min, health monitoring, email alerts on failure
- **Opening and closing odds preservation** — closing odds survive pipeline rebuilds correctly
- **Live odds** — The Odds API, best-price-across-bookmakers comparison
- **Vig-removed implied win probability** — working
- **H2H record** — via API-Tennis get_H2H, working
- **Surface-specific win rate** — working (filter by `type === 'singles'`, handle `""` as 0 not NaN — bug was found and fixed)
- **Name-matching logic** — between The Odds API and API-Tennis, tested on 4 real player pairs including hyphenated names (Auger-Aliassime, Struff), reasonably solid

### ⚠️ Built but untested / fragile

- **Weather integration** — `fetchMatchWeather` in `bsp-pipeline.js` via Open-Meteo. Built from official docs, never successfully called in production. Coordinates only added for Wimbledon so far in `VENUE_COORDS` object. **Test this before relying on it.** Open-Meteo free tier is non-commercial — check commercial API pricing before scaling.
- **H2H page surface filter** — known bug, surface filter does not work correctly

### ❌ Not started — these are the remaining priorities

1. **Value % scoring methodology — UNRESOLVED (this doc vs `design-export/README.md`; do not resolve without Michael).** Two different things wear one name:
   - *This doc's position:* the proprietary `value %` (W/UE ratio, surface form weighting, fatigue scoring) is Stennisfy's real differentiator, is `null` everywhere in the data, and must not be invented or approximated — ask Michael for the scoring logic before building it.
   - *The export's position:* the design already renders a value verdict ("SHARP VALUE" / "NO VALUE") derived from a **no-vig best-price margin** (`ppGap = (1/fair − 1/price) × 100`); the README flags the soft-book price inputs behind it as authored placeholders under "Known gaps".
   - These are not the same feature: a no-vig price-margin verdict (built, placeholder inputs) vs a proprietary W/UE model score (not built). Do **not** wire the proprietary methodology behind the rendered verdict, or ship the placeholder inputs as if they were the model, until Michael confirms which is intended.
2. **Playing-style classification** — "Aggressive", "Defensive baseliner", etc. No vendor provides this. Needs Michael's category definitions.
3. **Sackmann tennis_atp integration** — historical match results, W/L splits, surface splits, tournament records. Being integrated, not complete.
4. **Sackmann MatchChartingProject integration** — shot-by-shot data, serve/return stats, rally length. Being integrated, not complete.
5. **Subscription / paywall layer** — no auth or paywall logic exists yet
6. **Mobile responsiveness** — not addressed

---

## File structure

```
/
├── bsp-consult-dashboard.html     ← Main frontend (single file, all pages)
├── bsp-pipeline.js                ← Node.js data pipeline (runs on cron)
├── matches.json                   ← Output: today's matches + odds
├── tournament-profiles.json       ← Output: tournament data
├── tournament-progression.json    ← Output: draw/bracket data
├── player-profiles.json           ← Output: player stats cache
├── api-tennis-integration.js      ← Reference: API-Tennis endpoint shapes
├── backtest_elo.py                ← Internal Elo research tool (not production)
├── backtest_demo.py               ← Demo version, tested on 15 matches
├── demo_matches.csv               ← Test data for backtest
└── .github/
    └── workflows/                 ← GitHub Actions pipeline config
```

---

## Architecture rules — follow these on every task

1. **Never fabricate data.** Every stat, number, or chart must come from a confirmed real data source. If a field isn't available, show nothing or flag it — never approximate or invent.
2. **Feasibility before UI.** For any new data field, confirm it exists in the pipeline before writing display code. Flag gaps rather than filling them.
3. **Atomic writes only.** All pipeline output must use temp file + rename pattern — never write directly to live JSON files.
4. **Global fixes over local patches.** If a bug exists in multiple places, fix it at the source, not per-instance.
5. **Scope discipline.** Every task specifies what to keep unchanged — respect those boundaries exactly. Do not refactor, redesign, or touch anything outside the stated scope.
6. **Real data sources only.** Sackmann datasets are flat files, not live APIs — any integration must download, parse, and cache them locally, not query at runtime.
7. **ATP only.** No WTA content anywhere — filter it out at the pipeline level (`tourBadge === 'ATP'`).

---

## Data sources — confirmed status

| Source | What it provides | Status |
|---|---|---|
| api-tennis.com | Fixtures, results, H2H, surface stats, box scores | Live |
| The Odds API | Pre-match odds, Grand Slams / 1000s / 500s | Live |
| OddsAPI | Odds for ATP 250 and broader coverage | Live |
| Oddsapi | Additional odds depending on coverage | Live |
| Open-Meteo | Weather at venue | Built, untested in prod |
| Sackmann tennis_atp | Historical W/L, surface splits, tournament records | Integration in progress |
| Sackmann MatchCharting | Shot-by-shot, serve/return, rally length | Integration in progress |

**Model R&D mode (founder decision, TEN-8, 2026-07-26):** The h2h-model — including every Stage-2 adjustment layer (e.g. Layer #4 surface record) and the Layer #8 MCP archetype baseline — is being built and run in **internal research & development mode**. The CC BY-NC-SA non-commercial restriction is a gate on **commercial serving to paying members**, not on internal R&D. Do **not** flag the non-commercial data licence as a blocker for R&D builds — activate and iterate. The licence gate re-engages only when we move to **commercial deployment with paying members**, at which point a commercially-licensed alternative (or clean-room Stage-1-only fair price) is required before serving. Flag licensing then, not now.

**License warning — Sackmann datasets:** CC BY-NC-SA (non-commercial). Internal model R&D use is permitted (see **Model R&D mode** above). Do not serve this data, or a model trained on it directly, to **paying members** once the product is commercially deployed — revisit for a commercially-licensed alternative before commercial launch.

**License warning — Layer #8 W/UE MCP archetype baseline (`mcp-archetype-baseline.json`):** derived from the Match Charting Project (CC BY-NC-SA, non-commercial). Same rule as the model weights above — **internal R&D use is permitted** (see **Model R&D mode**); it seeds the W/UE-ratio expectation per archetype but must NOT be served commercially to paying members until we have a commercially-licensed alternative. Founder decisions (TEN-8, 2026-07-25; R&D-mode clarification 2026-07-26).

**Name matching note:** The Odds API and API-Tennis use different name formats. Current matching is by last name — tested against 4 real pairs. Watch for silent match-merge failures on busy match days.

---

## Design system — the export is canonical

### STANDING DIRECTIVE — design fidelity (founder ruling, 2026-08-02)

**The design is the specification, not a reference.** Michael designs this product in Claude Design; the export **is** what he designed. Your job is to reproduce it — not to interpret it, improve it, or substitute a reasonable equivalent.

1. **When the export and your instinct disagree, the export wins.** This already holds for measurable values; it also holds for anything that "looks fine either way."
2. **No generic-convention defaults.** Component-library styling, framework defaults and general design instinct are all *wrong here by default* — this system has specific rules that differ from convention. When you reach for a sensible standard treatment, that is the signal to stop and read the export instead.
3. **Rebuild rather than restyle.** If a section's structure differs from the export, adjusting colours and spacing on your existing markup will not converge — build the export's structure.
4. **Read values, never infer them.** `computed-styles.json` is authoritative for every measurable value — padding, margin, width, font size and weight, line height, radius, gap, fill alpha, gradient stop, glyph. No rounding. **No number quoted in a prompt — including the founder's — is a source; treat every quoted value as a hypothesis to verify against the export.**
5. **Per-tab specs outrank computed styles inside their scope.** Two exist: `design-export/specs/match-stats-tab-spec.md`, `design-export/specs/form-tab-spec.md`.

The three carve-outs where you still stop and ask are unchanged: **fabricated or absent data, scope with no data-layer counterpart, and product decisions that post-date the export.**

**Do not change** without the audit saying so: anything currently signed off; the half-pixel authored sizes (13.5, 12.5, 11.5, 10.5, 9.5 — real design values, source of the sub-pixel diff floor); the two blue systems (`#3e7bfa` legacy vs `#5b9bff` canonical — **frozen, do not migrate**); the handoff decisions list (removed Summary view, removed H2H trend chart, removed Extra Stats tab, uncomputed match time).

---

**Canonical source of truth:** the Framer design export under `design-export/` **is** the design. Read exact values from `design-export/computed-styles.json` (every element, every state — 30 page-states), the raw token set from `design-export/tokens-observed.json`, and the readable token set from `design-export/tokens-design.json`. The narrative reference is `design-export/README.md`. **Where this doc and the export disagree on anything measurable, the export wins** — on rules, scope and reasoning, the README wins. (This section was corrected on 2026-07-31 after CLAUDE.md was found describing a different product than the export; report any remaining conflict rather than guessing.)

**Per-tab dev specs sit above the export within their scope (founder handoff, 2026-08-01).** Where a tab has a dedicated handoff spec — `design-export/specs/match-stats-tab-spec.md`, `design-export/specs/form-tab-spec.md`, and any that follow, one per tab — that spec is **authoritative for that tab**: read exact values from the spec, and fall back to `computed-styles.json` for the tab's own page-state only where the spec is silent. Outside a tab that has its own spec, the export rules (as above). On a spec-vs-export conflict inside a tab's scope, the spec wins — note the divergence in your report and proceed.

- **Aesthetic:** Dark dashboard, near-black backgrounds (#06070a / #0a0d14), hairline borders (rgba(255,255,255,0.06–0.09)). Surfaces are mostly flat — but the build uses gradients, one shadow, and tinted washes **deliberately**. Do not strip them:
  - **Gradients** (present, correct): the Recent Form bar (`--brand-deep → --brand`); identity-tinted cards on the Tournament / Overview / Form tabs (`hexA(identity, 0.16) → --card`); the Completed-match odds-journey line (`--muted → brand blue`); the logo mark; the court-speed slider track.
  - **Shadow** (present, correct): the Match Analysis modal card — `box-shadow: 0 40px 120px rgba(0,0,0,0.6)`. This is the **only** elevation shadow in the build; everything else stays flat.
  - **Tinted washes** (present, correct): `--brand-wash` rgba(91,155,255,0.08), `--brand-tint` rgba(91,155,255,0.15), `--pos-bg` / `--neg-bg` value tiles, day-card fills. These are surface tints carrying meaning, not decoration — keep them.
- **Style reference:** Linear, Vercel dashboard, Stripe settings pages — confident typography, generous whitespace, clear hierarchy
- **Typography:** Five font weights are used, as built: 400 (regular, the bulk of text), 500, 600, 700, and 800 (headings, active pills, large mono figures). The export is canonical here — 600/700/800 across 7,500+ elements is the design, not a violation. Hierarchy comes from size and weight, not hue. (Verified against `design-export/tokens-observed.json`, 2026-07-31.)
- **Case:** Sentence case for prose, headings, body copy and nav. **All-caps is used deliberately** for utility/mono labels — section eyebrows, stat labels, date-group headers and meta ("STAT COMPARISON", "MATCH TIME", "OPEN / CLOSE", "30 JUL 2026", "n OF 17 VALUE LAYERS ACTIVE"), typically letterspaced mono. Never title case. Sentence case for content, caps for mono/utility labels — match the export.
- **Colour semantics — hue carries meaning, tone carries hierarchy.** Text hierarchy is built from grey tone-levels (`--text` #e7e9ee → `--muted` #5b6880 → `--muted-2` #4b5672 → body prose), never from hue. Hue is reserved for meaning: brand blue #5b9bff for interactive/identity, green #3dd68c / red #e0616f for value verdicts and data-quality, clay #e8a84e for the clay surface, per-player identity tints for *who* a player is. This is why "hierarchy through weight/size, not colour" and the graded grey text system agree — greys are tone, not hue.
- **Spacing:** Generous — data-dense but never cramped
- **Stat display:** Never use hue to indicate *who leads or won* between two players — head-to-head comparison stats stay neutral. (Single-player threshold colouring against a baseline, and value/data-quality verdicts, are a different axis and do use hue — see colour semantics above.) *Exception inline (Match Stats tab only, founder ruling 2026-08-01, narrowed 2026-08-01):* on the Match Stats tab, **tone — not hue — may mark outcome in the point-log game/tiebreak scores only** (winner `#e7e9ee` / other `#4b5672`) — grey tone-levels signalling outcome, permitted because *hue carries meaning, tone carries hierarchy* (line above). **The score-header names are NOT an outcome mark: both names are `#e7e9ee`, winner and loser alike** — the earlier winner `#e7e9ee` / loser `#5b6880` dim was reversed by founder ruling (the render never dimmed the loser). Outcome on the score header rides on the score panel itself (sets/per-set line), never on the two names. **Identity hue is unaffected:** player A stays `#6aaeff` and player B `#e7e9ee` by name order in the legend, values and bar fills, regardless of who won. Test: the two score-header names are always the same tone (`#e7e9ee`); the only permitted tone-marked outcome is the point-log game/tiebreak score; a player's identity blue/white must never change with the result.
- **Expandable panels:** All must have a visible close/dismiss control
- **Charts:** Preferred over tables for comparison views where possible

---

## Rulings ledger — apply these as tests

**Standing process (founder ruling, 2026-08-01).** Every ruling the founder gives is written into *this file* as a rule the **same day** it is given — not only applied to the tab in front of you and logged in BUILD-NOTES. CLAUDE.md loads automatically every session; a ruling that lives only in a chat has to be re-asked next time. When you record one:

- **State it as a test someone can apply, not a prohibition.** Prohibitions generate edge-case questions; tests resolve them. Model: *"blue answers whose number this is, never whether the number is good"* beats *"never use blue on a performance value"* — the second one made us litigate whether an ace counts as performance.
- **List exceptions inline with the rule**, not in a separate section, so nobody applies a rule without seeing its carve-outs.

### Database tab — yield-by-odds-band window & method (TEN-146, founder ruling 2026-09-04)

The Database tab (`build-database-yield.js` → `database-yield.json` + lazy `database-yield-players.json`, rendered in `bsp-consult-dashboard.html`) is built from the Tennis-Data ATP closing-line archive (`odds-archive/*.csv`) under these LOCKED rulings — apply as tests, do not re-litigate:

- **Window: season ≥ 2010, fail-closed, NO pre-window fallback (Option A).** 2004–2008 is a materially different sport; a blended yield across it prices a market that no longer exists. This is a *regime* cut, not a coverage cut. Pre-2010 rows that would otherwise be usable land in the `preWindow` reconciliation bucket. **Test:** `meta.windowStart === 2010`; `meta.dateRange[0] >= '2010-01-01'`; `meta.used === 41667` on the current archive; `meta.exclusions.preWindow === 13659`; and used + all six exclusion buckets (walkover 344 + edge 8 + noResolvingBookPrice 3502 + exactTie 248 + overroundGt115 5 + preWindow 13659) === `meta.archiveRows` 59433 exactly. A build whose date range starts before 2010, or that fills pre-window years with a fallback book, is wrong.
- **Book: Pinnacle closing for every season except 2026; Bet365 for 2026 only (seam-marked, never blended into an unlabelled figure).** Split yields are reported per book. **Test:** page label reads "Pinnacle closing prices, 2010–2026", never "all years"/"2004"; a curve spanning 2026 carries a seam marker + caption; fav/dog yields shown Pinnacle (n=40002) and 2026-Bet365 (n=1665) separately.
- **Fav/dog by price** (shorter price = favourite); **exact ties excluded** (no ranking/positional tiebreak); **overround > 1.15 excluded**; **retirements included, walkovers + edge non-results excluded**; **bands = dynamic equal terciles recomputed inside the active filter** (at the default filter, 41667/3 = 13889 per band, counts identical). **Scope: ATP only, permanently — no WTA toggle.**

### Default resolution rule (founder ruling, 2026-08-01)

When the export and a documented rule disagree on anything **measurable — spacing, colour, weight, radius, anatomy, layout, ordering — the export wins and you proceed.** Note the divergence in your report so it stays visible, but do **not** hold work waiting on a ruling. (This replaces the old "flag both and never pick." Flagging still happens; waiting does not.)

**Three carve-outs where you still stop and ask, because the export cannot settle them:**

1. **Fabricated or absent data.** If a value isn't in the source, no export mock justifies inventing one. Em dash and report.
2. **Scope.** If the export shows a section, control or binding with no counterpart in the build's data layer, report before building it.
3. **Product decisions that post-date the export.** The export is a snapshot; some things were deliberately added or removed after it. Before you delete something *because the export lacks it*, ask first.

### Rulings — each is a test

- **The Database cumulative-profit charts draw at the EXPORT's vertex budget, and the down-sample MUST PRESERVE THE ENVELOPE (founder ruling TEN-196, 2026-09-13, item 1, + its clean-context review the same day).** *"The mock-up graphic you took is well coded in the graphic but not when you add the filters, the graphic is going everywhere and is not how it is on the design export."* `design-export/database-handoff/Database.dc.html` builds every curve with `curve(n, …)` and lays it out at **exactly n evenly-spaced x**: **260** Tour, **240** Tournaments, **220** player main, **200** player sub. The old sampler bucketed at the viewBox's full **1/1000** resolution keeping `first/min/max/last`, so it drew a **vertical riser inside every occupied bucket** — measured **3,059 vertices against the export's 260** unfiltered, and worst under `Level = Masters 1000`, which is **nine event weeks a season, not a stream**. **THE OBVIOUS FIX IS WRONG AND WAS CAUGHT IN REVIEW BEFORE IT SHIPPED. Do not reinstate it.** Resampling at N evenly-spaced x and step-holding the value keeps no extremum between samples, so it **FLATTENS PEAKS**: the **Australian Open underdog curve really reaches +4.4u and step-hold painted its maximum at −3.8u** — *"never once in profit"* on a page whose whole claim is the archive — on **24 of 169** event curves. It also breaks the axis, which is scaled from the **TRUE** extrema: **Masters 1000 underdogs peak at +58.8u and step-hold drew +43.8u**, so the ink never reached the top labelled gridline and reading the max off the chart understated it by **26%**. **What ships instead:** `poly(xs, vals, Y, N)` buckets at **N/2, not 1000**, and keeps each bucket's **highest and lowest** point in index order, with the true first and last pinned. Budget honoured, every extremum survives, no zero crossing can be flattened, and the curve always reaches the bounds the gridline ladder was built from. **Under N points every real point is drawn** — 119 of 169 events are point-for-point exact; we never fabricate a vertex to reach the export's count. **The x basis is now MATCH INDEX, not date** (founder ruling 2026-09-13 round 3, `chart-x` → `match-index`; the line below supersedes the old C3 date basis) — and no painted number is derived from the curve: Tour ends `-894u` / `-2,268u` and Masters 1000 `-311u` / `-55u` across every version. Test: on Tour (no filter), Tour + Masters 1000, and Tournaments + Australian Open, invert the painted y against the painted axis labels and assert the drawn max and min equal the independently recomputed true max and min to within 0.6u, that the SIGN of the peak is preserved, that vertex count is within budget, and that x never goes backwards.
- **The three Players charts DRAW vertical season gridlines (founder ruling TEN-196, 2026-09-13, item 2 — "add them, the export wins").** This one cuts *against* the minimalism of item 1 and he ruled it anyway. The export draws one vertical per season tick on **both** the player main chart and the two pair charts (`Database.dc.html` → `playerMain.ticks` and `c.ticks`), `rgba(255,255,255,0.05)`, full plot height. The page drew **none**. Test: all three `.db-pcarea svg` report ≥1 line with `x1===x2`, at 0.05, with and without a Level filter.
- **The two Database cumulative-profit curves carry DIFFERENT COLOURS as well as different line styles (founder ruling TEN-196, 2026-09-13, item 1).** *"Favourites and underdogs are both blue, separated only by solid versus dashed. On a dense chart where they cross constantly, dashing alone doesn't do it. Keep solid/dashed as well — colour plus line style is more robust than either alone."* The export ships `#5b9bff` / `#4db8ff`, measured **CIEDE2000 10.9** apart (hue 280.3deg vs 259.1deg) — **this is one of the places the export does NOT win**, and it was overruled on a measurement, not a preference. What ships: `COL_FAV='#5b9bff'` / `COL_DOG='#e7e9ee'`, **dE2000 30.0**, contrast **7.0:1** and **16.0:1** on the `#0a0d14` panel. `#e7e9ee` is the export's own primary-text token, not a new colour. **THREE TOKENS ARE RESERVED AND MUST NEVER BE USED FOR A SERIES** — `#3dd68c` (positive yield), `#e0616f` (negative yield) and `#e8a84e` (the book-seam marker, drawn on this same chart); borrowing any of them makes a curve read as a yield sign or a seam. **The dash is NOT a substitute and must not be dropped** when the colour changes: the underdog polyline keeps `stroke-dasharray="5 3"` and the legend keeps one solid and one dashed swatch. Test: read `getComputedStyle` off both painted polylines — assert the strokes differ, that dE2000 > 20 (the pair it replaced measured 10.88, so a nudge must fail), that the favourite dash is `none` and the underdog dash is non-empty, that both clear 3:1 on the panel, that neither is within dE 5 of the three reserved tokens, and that the two legend swatches match the two strokes and are `solid` / `dashed`.
- **`Finals` is folded out of the TOUR tab's Level picker too, not just Players (founder ruling TEN-196, 2026-09-13, item 3).** *"Fold from the picker, keep in the archive; `All` still counts them."* **238 rows, 0.57% of the archive, every one of them `Masters Cup`** — and `Masters Cup` stays fully reachable by picking it as a tournament on the Tournaments tab, so nothing becomes invisible. Both pickers now share ONE definition, `foldedOpts(dict, expected, folded)` + `LEVELS_EXPECTED` + `FOLDED_LEVELS`, so Tour and Players cannot drift apart. It **fails CLOSED**: the dictionary is asserted whole and the control is DROPPED if it has moved, rather than offering an unruled set. **The all-checked collapse is GONE — see the `no-collapse` rule below; the reasoning that once justified it here was also wrong** (`st.size===0` already reaches `All`, so nothing was ever made unreachable). **A SOURCE REGEX FOR "Finals" IS A VACUOUS TEST HERE** and would pass against a build that deleted the 238 rows outright. Test by MUTATION: read the `All`-row match count (41,667), check ONE level and assert it drops to that level's own recomputed count (Grand Slam = 8,262, which proves the picker actually bites), then check every remaining offered level and assert the count returns to **41,667 — i.e. 238 MORE than the four offered levels sum to (41,429)**. That difference IS the proof that the fold is a picker change and not a data change.
- **A DERIVED EXPECTATION CANNOT DETECT A CHANGE IN WHAT IT DERIVES FROM — pin the literals (clean-context review, TEN-196, 2026-09-13).** The mutation test above was written to defend against exactly the deletion it then failed to catch. `ten196-items13-probe.mjs` computed `totalAll`/`totalFolded`/`totalOffered` from the same artefact it tested, so the review built an artefact with **all 238 Finals rows deleted but `'Finals'` still in `meta.levels`, and the probe passed 22/22** — the headline assertion `afterAllChecked - totalOffered === totalFolded` had degenerated to `0 === 0`, and `totalFolded` was printed but never asserted. **This is the general trap, not a one-off**: a probe that recomputes its expectation from the artefact proves the PAGE agrees with the artefact and says nothing about whether the artefact is still right. Fix, and the pattern to copy: assert the headline counts as **hard-coded literals** (`PIN_ALL=41667`, `PIN_FOLDED=238`, `PIN_OFFERED=41429`) as PRECONDITIONS before any derived assertion runs. Verified by re-running against the same mutant: **2 explicit failures naming the defect**, where it previously passed clean.
- **The Players Level filter offers FOUR levels; Finals is folded away (founder ruling TEN-196, 2026-09-13, item 3 — option `build-4`).** Finals is **238 matches for the whole tour across 16 seasons**, thin by construction for almost every player. It is **not offered as a choice** and it is **NOT removed from the archive** — default `All` still counts it, so no match is hidden from a player's unfiltered record. `playerLevels()` **asserts the whole level dictionary** and returns null if it has moved, dropping the control entirely; it does NOT filter by name, because a name filter fails OPEN on a rename and would silently offer a fifth level (corrected 2026-09-13 — this line previously claimed the opposite of the code). **Single-select**, matching this tab's own Surface/Tournament controls, which also means *"all four picked"* can never be mistaken for *"All"*. **The tour baseline MUST recompute inside the filter** — *"comparing a player's Slam record against an all-levels baseline would be the wrong denominator, and that's exactly the error class we've been clearing out."* `playerBaselines()` carries the same `state.plevel` predicate as `playerRecs()`. Test: the Level menu lists exactly `All, Grand Slam, Masters 1000, ATP 500, ATP 250`; `Khachanov K.` at `Grand Slam` prints `+2.87%`, record `77–37`, **`Tour -6.81%` and not the all-levels `-3.79%`**, with gaps `+6.36pp` / `+9.05pp` against the *filtered* baseline.
- **[SUPERSEDED 2026-09-13 round 3 — the alias layer is BUILT; see the `common-plus-both-search` rule below. The fragmentation facts here still hold.]** **Tournament aliases and merges were TEN-199, and the alias build was HELD behind the reviewed table (founder ruling TEN-196, 2026-09-13, items `alias-display` → *common name only*, `alias-scope` → *authorise a reviewed merge table as a separate task*).** The archive's **169 name strings are not 169 events**: **40 venues are fragmented** across 2–4 strings, **15,491 matches (37.2%)**. Displaying common names alone *today* would put **two rows both labelled "Miami"** in the picker with nothing to tell them apart — a new ambiguity, not a fix. **The merge table cannot be generated** — deriving it from level + surface + calendar week + disjoint seasons over-merges Moscow, Cologne, Florence and Stockholm into one group. Do not hand-wave an alias map into this page; the reviewed table lives on **TEN-199** (document `merge-table`).

- **The Database page carries TWO x bases and that is deliberate: the Tour and Tournaments charts plot MATCH INDEX, the three Players panels plot CALENDAR DATE. Neither may be captioned as the other (founder ruling TEN-196, 2026-09-13 round 3, `chart-x` → `match-index`, then the round-3 scope call `player-charts` → `tournaments-only`).** I shipped match index on **every** chart on the page and asked, arguing that two bases on one page is worse than either; **the founder overturned it and the reasoning is his, not mine**: the defect match index exists to fix is a **property of one-week-a-year events**, not of the page. A player plays year-round, so a Players panel has almost no dead air to remove and nothing to gain — while the **vertical season gridlines ruled in the round before** only stay **evenly spaced** on a time axis. On the index basis an idle stretch occupied no width and a heavy season occupied more, so those gridlines bunched. **Do not "harmonise" the two bases back together.** The Players domain is `d0` = **1 Jan of the first season with a match** (a season boundary — the C3 fix; anchoring at the first match date and then CLAMPING the first tick draws that label at a position the date does not occupy. **The "4.39pp" this rule used to quote is WITHDRAWN** — neither a clean-context review nor I could re-derive it from the archive, on a player or on an event, so it was carried memory, not a measurement. Re-measure before quoting a figure here) to `d1` = the **archive's** last date, not the player's, so a retired player's curve ends where he stopped instead of being stretched to full width. All three panels take the **same `d0`/`span`**, which is what keeps *"shared scale"* true — on a date basis that is by construction and needs no base-series lookup. `calendarTicks(d0, span, steps)` and `seasonTicks(ordered, yearOf, steps)` are **two functions, not one with a mode flag**. `calendarTicks` **drops** an out-of-range tick rather than clamping it, because a clamped tick is a label pointing at a date it does not mark — but note that guard is **defensive only and no real input reaches it** (`d0` is always 1 Jan, so `f=0` exactly at `y0`, and Jan 1 of `y1` cannot exceed `d0+span`; measured **0 drops across all 1,129 players**). **Do not write a test that claims to exercise it.** **THE COST OF THE CALENDAR BASIS IS REAL AND IS NOT "a player plays year-round, so there is little dead air" — that was my justification and a clean-context review took it apart.** True only of the *unfiltered* view of an *active* player; this tab has four filters. Median player with >220 matches paints **145 vertices against the index basis's 219**; **Nadal + Surface=Grass puts 58 points into 29 of 1,000 columns**, a *tighter* concentration than the Australian Open's 58/1,000 that moved the Tournaments charts off this very basis; a career gap costs more again (Giron's widest painted gap **313 columns** against 7). No extremum is lost either way (`poly` keeps each bucket's min and max), so this is **fidelity, not a wrong number** — but do not re-litigate the axis on the strength of the softer claim. Test: the Players eyebrow contains **"season axis"** and **no** "match index"; the `.db-xcap` on Tour/Tournaments still reads **"Match index (chronological) · ticks mark season starts"**; each Players tick's x equals `(Date.UTC(y,0,1)/86400000 − d0)/span` recomputed independently; the Players season gridlines are **uniformly spaced to within ONE DAY OF WIDTH (`100/span` pp), NEVER a fixed pp figure** — a leap year is one extra day, so a fixed **0.05pp** threshold is **unsatisfiable and fails 302 of 2,064 real ladders** (`Walton A.`, debut 2024, spreads **0.107pp**) while the worst spread anywhere is exactly **1.0000 day**. **Add a 0.02pp rounding term**: the page writes `style.left = (x*100).toFixed(2)`, so the painted ladder is quantised to 0.01pp and a spread-of-gaps carries +/-0.02pp of it — asserting below that measures the rounding, not the layout (the true spread for `Nishikori K.` is 0.0165pp; the DOM reads 0.0300pp). and the same tolerance must still **reject** the index ladder, asserted on the **Tour** chart (spread **3.76pp**), **NOT on an event** — a single event plays roughly the same number of matches every season, so the Australian Open's index ladder is only **0.376pp** off uniform and would read as near-passing on the basis the test exists to reject. The sub-panel assertions at the end of this rule (sub-range, and sub ticks landing on the main panel's) belong to **these Players panels**, since only they have sub-panels. **Everything else below governs the Tour and Tournaments charts only.** *"The tournament charts look like a child's drawing."* Measured before anything changed, and the mechanism was **not** the down-sampler and **not** a step function: the Australian Open series carries **2,130 real points**, but on a CALENDAR axis a one-week-a-year event puts them into **58 of 1,000 columns**, and because `poly()` buckets by **x**, only ~21 of its 119 buckets ever receive a point. **The deployed page painted 42 vertices out of 2,130.** The calendar axis was *starving the sampler* — `poly()` buckets by **x**, so with every point crammed into 58 columns only ~21 of its 119 buckets ever received one. **An earlier report of mine said the sampler was not involved; that was measuring the MEDIAN event, which is under budget and drawn point-for-point. The Australian Open is not, and it was the chart under discussion.** On match index the same curve paints **240 vertices across 235 columns**, widest gap **10.8** against **65.2**. It also **FIXES C3** instead of regressing it: in date order the first Bet365 row and the first 2026 row are **both index 40,002**, so the orange seam and the "2026" tick are the identical x by construction — measured **0.0000pp** against **0.0496pp** on the calendar axis. **The honest cost, which the caption must carry:** the axis is no longer linear in time, so a busier season takes more width and an idle stretch takes none. `.db-xcap` reads **"Match index (chronological) · ticks mark season starts"** — the export's own wording on its other profit chart. **It must never claim to be a time axis while being an index axis.** `seasonTicks(ordered, yearOf, steps)` places each label at the index of that season's **first match**, so only seasons that hold a match get a tick (an empty season occupies zero width; labelling it would put two labels on one pixel). (The Players panels no longer use this ladder — see the two-basis clause at the head of this rule. Their *"shared scale"* guarantee survives the change intact: one `d0`/`span` for all three panels means 50% across the Favourite panel is the same **date** as 50% across the Underdog panel, and a sub-panel still occupies a **sub-range** rather than being restretched. The orphan guard that the index basis needed went with it, because a date needs no base-series lookup.) Test: the caption matches exactly and contains no bare "Season"; the seam `x1` equals the "2026" tick's `left` to within 0.01pp; the Tour and AO curves start at x=0 and end at x=1000; the AO curve occupies >200 distinct columns with no gap wider than 15; ticks are strictly increasing; a sub-panel occupies a **sub-range** of 0–1000 rather than being restretched to fill it, and its season ticks land at the same x as the main panel's.
- **Round Robin is folded out of EVERY Round picker (founder ruling TEN-196, 2026-09-13 round 3, item 4 — his words: *"Remove Round Robin from all filters"*).** **191 rows, and every one of them is `Masters Cup`, i.e. a STRICT SUBSET of the 238 `Finals` rows already folded out of the Level picker.** It is not tidying: before this, `Round = Round Robin` on the Tour tab selected a slice that was **100% the folded level while the Level trigger still read "All"** — the other half of item 3's door. Same `foldedOpts()` definition, same fail-closed dictionary assertion, `ROUNDS_EXPECTED` / `FOLDED_ROUNDS`. The dictionary is **insertion-ordered, not draw order** (`1st, 2nd, QF, SF, Final, 3rd, 4th, Round Robin`) — assert it in that order, not in `ROUND_ORDER`. Rows stay in the archive and `All` still counts all 191. **A source regex for "Round Robin" is a VACUOUS test** and would pass against a build that dropped the 191 from `All`. Test by MUTATION: tick all seven offered rounds and assert the All row reads **41,476 — exactly 191 fewer than the unfiltered 41,667** — then clear every box and assert it returns to 41,667.
- **Checked boxes mean EXACTLY those boxes — the all-checked collapse is OVERTURNED (founder ruling TEN-196, 2026-09-13 round 3, `all-checked` → `no-collapse`).** `multiMenu` now does `setter(st.size===0 ? null : st)`. The collapse predated the folds and was harmless only while all-checked and `All` described the same set; they stopped doing so the moment a row was folded out, and the four ruled levels became **impossible to construct as an explicit set**. Now: four Levels ticked → **41,429**, trigger reads **4**; seven Rounds ticked → **41,476**, trigger reads **7**; the difference from `All` is exactly the folded rows (**238** and **191**). **`All` is still reachable** by clearing every box. **The rule is applied UNIFORMLY, not special-cased to Level** — Surface has nothing folded out, so all-three-ticked now reads "3" rather than "All" and returns the same 41,667. Test: all-offered-ticked on Level reads 41,429 with a trigger of 4; on Round 41,476 with a trigger of 7; clearing every box returns 41,667 on both.
- **The tournament alias layer is DISPLAY + SEARCH ONLY, and it must never change grouping (founder ruling TEN-196, 2026-09-13 round 3, `alias-display` → `common-plus-both-search`).** *"Common name displayed; search matches BOTH names and the city."* **169 strings in, 169 groups out. No match moves between events and no yield moves.** `state.tournament` still holds the **official archive string**, so filtering, bookmarks and stored state survive any alias edit. Source of truth is the hand-authored, machine-checked `merge-table` document on TEN-196: **163 of 169 aliased, 6 held, 95 rows carry a season range**. Display is the common name (`BNP Paribas Open` → **Indian Wells**, `French Open` → **Roland Garros**, `Masters Cup` → **ATP Finals**); search matches the official string, the common name and the city, diacritic- and punctuation-insensitive. **Where a venue is split across strings, BOTH rows show their season range** — `Miami 2010–2019` / `Miami 2021–2026` — so the split stays visible instead of becoming a silent duplicate; ranges are computed live from the rows, never hard-coded. **A RANGE CANNOT DISAMBIGUATE TWO STRINGS THAT RAN IN THE SAME SEASONS, AND THREE PAIRS DO** (clean-context review, 2026-09-13 round 3): `bett1HULKS Indoors`/`bett1HULKS Championship` (Cologne 2020), `Great Ocean Road Open`/`Murray River Open` (Melbourne 2021), `Adelaide International 1`/`2` (2022–23). Without a second pass they paint the **identical label**, and behind the two Cologne rows sit favourite yields of **−28.78% and +12.11% — a 40.9pp spread under one name**. A final pass appends the official archive string to any label that is not unique (`Cologne 2020 · bett1HULKS Indoors`). **Invariant: 169 strings → 169 DISTINCT labels. Assert it.** The **rendered label is searchable too** — before the review it was not, so typing what was on screen (`Miami 2010`) returned "No tournament matches that." on all 95 ranged rows. And an **empty NORMALISED query is not an empty query**: `nrm('&')` is `''`, which made `&` match every event; it falls back to a raw substring match instead. The ambiguity the founder flagged is now the feature: "BNP Paribas" returns two rows labelled **Indian Wells** and **Paris**. **SIX STRINGS ARE DELIBERATELY UN-ALIASED and show their official name** — `International Championships`, `Portugal Open`, `Astana Open`, `Movistar Open`, `VTR Open`, `Royal Guard Open Chile` — because an alias is a *claim about what an event is* and those need the founder's knowledge; absent, they claim nothing. **This layer fails OPEN by design, and that is the OPPOSITE posture to the Level fold — do not "fix" it to fail closed.** A string in neither list shows its official name and is named once in a console warning: the risk of an alias is a **wrong label**, while an unaliased row is merely unlabelled, and the archive gains events every season, so a fail-closed layer would blank the whole picker for one new string. **TEST THE GROUPING ON A *SPLIT* VENUE, NOT A 1:1 ONE — this is where the first probe was blind.** Its only two grouping assertions used Indian Wells and the Australian Open, both 1:1 venues with no sibling string, so **no merge bug could move either**: the review built a mutant keying `filteredRows` on the common name (Miami 930→1,492, Barcelona 485→728, Cologne 27→54) and it scored **57 of 57**. One of the assertions was even *named* for Miami while asserting on Indian Wells. Fixed by asserting per-string counts on the split venues; re-run against the same mutant it now fails with **5 explicit failures**. Test: "Indian Wells" finds the event; "BNP Paribas" returns two rows labelled Indian Wells and Paris; "Miami" returns both rows with their ranges; typing a painted label finds its row; "Toronto" finds Canada; "Kitzbühel" finds Kitzbuhel; "Movistar" returns `Movistar Open` and nothing mentioning Viña del Mar; **`Sony Ericsson Open` paints 930 and `Miami Open` 562, each its own string's raw-CSV count**, Indian Wells 1,486, the Australian Open 2,130, and the 169 labels are distinct.
- **The Database top band prints its REAL upper bound — the export's `Super Dog → from + '+'` rule is OVERTURNED (founder ruling TEN-196, 2026-09-13).** *"Super Dog prints `3.75+` where the real top end is 86.00. Overturn it — show the real boundary. On a page whose whole claim is 'our own archive, real closing prices,' a band that hides its own top end is the one element not telling you what it knows."* `bsp-consult-dashboard.html` band-row builder prints `fmtP(b.hi)` unconditionally; `b.hi` is the band's own highest closing price. **Tour reads `3.75 – 86.00`, the Australian Open `5.61 – 81.00`, Level = Grand Slam `5.61 – 86.00`.** **THREE WRITTEN SOURCES STILL SAY THE OPPOSITE and a future run WILL find them first:** `design-export/database-handoff/README.md` still specifies `from + '+'` (with the rationale *"the raw top end is a single outlier price, not a meaningful bound"*), `design-export/database-handoff/Database.dc.html` still implements it, and this file's default resolution rule says *"the export wins and you proceed"* on anything measurable. **This ruling overrides all three for this cell.** Do not reinstate the `+`. **Reported to the founder and not hidden:** the 86.00 is ONE match — 2011-09-02, US Open R2, fav 1.01 / dog 86.00 on Pinnacle, overround 1.0017, a degenerate quote sitting on Pinnacle's 1.01 floor that clears the `overround > 1.15` filter; 4 archive rows are ≥80 and 21 are ≥70. He ruled with that on the table. **Also latent, measured not estimated:** the `From`/`To` grid tracks are a fixed **40px** and `.db-gr > div` sets no `overflow`/`text-overflow`; `86.00` renders **39.0px — 1.0px of slack** (identical to the `3.75+` it replaced, so this change is width-neutral), but a 6-glyph price such as `101.00` measures **46.8px and spills 6.8px** into the `From` column. Unreachable from today's archive (max dog price 86.00); widen the track before any archive that could carry one. Test: with no filters the dog panel's third row prints `3.75` / `86.00`, no trailing `+` survives in any band `To` cell on any tab, the boundary MOVES when Level or a tournament is picked, and the cell reports 0px of overflow.
- **Never land on an empty day (founder task TEN-179, 2026-09-11).** *"Nobody wants an empty page."* Entering a Matches mode — the `Upcoming|Completed` control, a `#Results` deep link, or the first paint — must land on the nearest rail day that actually has matches: **Completed searches backward first, Upcoming forward first**, each falling through to the other direction. Only a rail that is dry in **both** directions keeps the empty state, and the date strip must show where it actually landed, never `Today` over an empty board. `mxLandOnNearestDay()` / `mxDayCount()`. **This is a LANDING rule, not a render rule** — an explicit chevron step or day-tab click must still be able to select an empty day (otherwise the rail is unusable and hides which days are empty), and `loadMatches()` is the **3-minute background refresh**, so it may land only ONCE and only when `matches.length` is non-zero (a failed first load must not burn the one shot). Test: switch to Completed on a day with no settled matches → the rail moves to the most recent settled day; empty the board entirely → the empty state is still shown with `Today` selected; step the chevron onto an empty day → it stays there.
- **`mxDayCount()` must call the real `getFiltered()` with `state.day` swapped in — never a second copy of its predicate.** The day/view interplay (a past day shows completed cards in Upcoming, a future day shows upcoming cards in Completed) lives only in `getFiltered`; a duplicated rule drifts out of step silently. Restore `state.day` in a `finally`.
- **`BIGGEST MARKET MOVE` is a toggle, not a one-way trip (founder task TEN-179, 2026-09-11).** A second click returns the board to the default sort, which is what restores the form bars — the card's right-hand block reads straight off `state.sort`. The tile shows a selected state (`.is-on` + `aria-pressed`) while active. Test: click twice → `state.sort` is back to `time`, the form bars are back, and the tile is no longer lit. The keyboard path must re-`focus()` the tile after the render, because `renderMatches()` rebuilds `#mcSummaryStrip` wholesale and focus would otherwise fall to `<body>` with no way to toggle back.
- **A Completed card opens the modal on Match Stats (founder task TEN-179, 2026-09-11).** The whole card is the hit target; the **modal itself is never modified** — route through its existing `aGoTab('matchstats')`. Player names/avatars, the Market Signal chip, the **expanded signal panel** (all `div`/`span`, so a `closest('a, button, …')` guard does NOT cover it) and an active text selection all stay inert. Upcoming cards are unchanged and add no tab stop. **When a container carries `role="button"` for its keyboard path, a delegated "is this an interactive child?" guard must compare the `closest()` hit against the container** (`ctl !== card`) — otherwise it matches the container itself and the feature silently never fires.

- **Blue.** Blue (`--brand #5b9bff`) answers *whose* number this is — interactive control or player/tournament identity — never *whether* the number is good. Test: if the blue is signalling quality/performance, it's wrong; recolour or neutralise. *Exception inline:* the legacy blue `#3e7bfa` (`--accent`, `--mx-brand-blue`) is frozen — do not migrate it to `--brand` piecemeal, only on explicit founder go-ahead. (The archetype dominance / matchup-edge bars are **not** a blue exception — those are a directional edge and take green/red; see below and [[ten8-playing-styles-export-colors]].)
- **Green / red.** Green (`#3dd68c`) / red (`#e0616f`) answer **"is this value trustworthy"** or **"which direction did this move"** — **never "which player is better."** Direction is permitted only where the thing being measured is *itself* directional: an odds-movement delta, a matchup edge, a drift. A count or rate placed beside another player's count or rate is a **comparison**, and comparisons take identity colour or neutral, never green/red (so H2H comparison stats stay neutral). Test: is the colour answering trustworthy-or-direction, or is it ranking two players? If it ranks players, it's wrong — neutralise. *Exceptions inline — canonical tokens `#3dd68c`/`#e0616f` only, on canonical values only:* (a) Playing Styles **matchup edge and dominance** (grid + bars — the page's subject is strong-vs-weak and the edge is itself directional); (b) **Odds-tab movement deltas**; (c) **LOST SERVE and BP markers**; (d) the Clay surface tag never renders green — it is orange/terracotta. The former app-wide near-miss hues `#3ECF8E`/`#E8607A` are **retired** — migrated to the canonical tokens in commit `f497400` (67 sites across dashboard, funnel, verify). If either near-miss hex reappears in a UI file, it's wrong.
- **Modal tab rail.** The Match Analysis modal has **exactly eleven tabs** and **News is second**. Rail order: Key factors, News, Playing style, Form, H2H, Match Stats, Progression, Overview, Tournament, Weather, Odds. Test: count = 11 and position 2 = News; if not, it's wrong.
- **Overview tab — identity vs outcome (founder ruling 2026-08-01).** Two distinct axes coexist on the Overview modal tab. **(1) Identity by name order:** the career-card surface bars, the `THIS SEASON · BY SURFACE` row left-accent, and the nested match-stats block's left name carry *whose column this is* — left player (P1) blue `#6aaeff`, right (P2) neutral `#e7e9ee`, fixed by name order, **never** by clay/hard/grass and never by value. (Reverses the earlier "career bars all-neutral / accent surface-family-blue" calls.) **(2) Outcome as a data fact:** the `W`/`L` result letter in the surface drill-down and match lists is coloured by outcome — canonical W `#3dd68c` / L `#e0616f` — and a `ret.` suffix carries the loss red (`w/o` stays neutral). A completed match's result is a data-fact verdict, not a two-player comparison, so it is a permitted green/red exception alongside LOST SERVE/BP markers (reverses the earlier "W/L neutral in modal" call). Test: surface bars/accents/left-name never change hue with the *value* (identity only); the W/L letter never stays neutral on a completed match.
- **Nested Match Stats block (Overview drill) is tertiary.** The shared `.aform-*` form panel, when it appears inside a match-row drill inside the Overview career card (`.yr-drill` scope), reads one step **smaller** than the Form tab / Player Profile (which are primary surfaces and keep full size). Its active view chip is **flat** — segmented-control standard `rgba(91,155,255,0.22)` fill + `#e7e9ee` text, **never** the solid `#3E7BFA` fill (reserved product-wide to Login-primary / Verify). Test: the Overview nested block is visibly smaller than the standalone Match Stats tab and its active chip carries no solid bright fill.
- **Strong text (`--sf-text-strong: #fff`, founder ruling 2026-08-01).** `#fff` is the **brightest text tier**, reserved for **active-state accents** — the label of an active segmented-control pill on a brand-tint fill, and similar selected-state controls. Body/label text stays `#e7e9ee` (`--sf-text`). **Test: if it's the *selected state* of a control, it's `#fff`; everything else is `#e7e9ee`.** The export settles the pill case: the active `span.seg` leaf computes `rgb(255,255,255)`/700 on the tinted fill in both matches-upcoming and matches-completed states, while the inactive sibling stays `#5b6880` — a deliberate accent, not a `#e7e9ee` artifact. **Token, not migration:** a sweep found **786 raw `#fff` occurrences** in the export — an unnamed brightest tier the palette never documented. Do **not** migrate those 786 sites; the token `--sf-text-strong: #fff` is defined (dashboard `:root`) so **new work** references the token instead of a raw hex. (Superseded the placeholder name `--active-pill-text`.)
- **Font weight.** A weight is valid iff it is one of **{400, 500, 600, 700, 800}**. Test: any other weight is wrong. Heavy use of 600/700/800 is the design (verified against `tokens-observed.json`), not a violation — do not "tone it down".
- **Gradients, shadows, tinted washes.** These are permitted **exactly where the export uses one** — apply where the export element carries it, remove only where the export element doesn't. Test: does the corresponding export element carry the gradient/shadow/wash? Match it. *Exception inline:* the only elevation shadow in the build is the Match Analysis modal card (`box-shadow: 0 40px 120px rgba(0,0,0,0.6)`); every other surface stays flat.
- **Em dash vs zero.** A field with no value renders an em dash **"—"**, never `0`; a field whose value is genuinely zero renders **`0`**, never an em dash. Test: is the value absent or is it really zero? They are never interchangeable, on any surface.
- **One container per content block.** Each content block is wrapped in **exactly one** container — no nested duplicate card/panel/border wrappers around the same block. Test: strip to a single enclosing container per block; if a block sits inside two cards, collapse to one.
- **Empty states fabricate nothing.** An empty state shows only what the data actually is — an em dash, a "no data"/"not measured" label, or nothing — it never invents, approximates, back-fills, or falls back to a placeholder number. Test: every value in the empty state must trace to real source data; if it can't, it must be an em dash or an explicit unavailable label.
- **Match-detail view toggle — no Summary view (founder ruling / form-tab-spec §4, 2026-08-01).** Every nested match-detail instance — the standalone modal **Match Stats** tab, and the expanded-row detail inside **Form**, **H2H** and the **Overview** career drill — offers exactly two views: **Stats** and **Point by point**. The **Summary view is removed product-wide**; the API carries no match-duration data, which was its only unique content. Test: a match-detail view toggle shows *Stats | Point by point* and nothing else — a Summary button anywhere is wrong.
- **H2H trend chart removed (founder ruling 2026-08-01).** The Match Analysis **H2H tab** does **not** render an `H2H TREND` block (the SVG timeline of every meeting on a shared baseline, its legend, axis labels, `tick height = sets margin` caption and running-lead line). It was removed as a product decision that post-dates the export — the export still renders it. This is the **third** such export-has / product-doesn't divergence, alongside the removed **Extra stats** tab and the removed match-detail **Summary** view. Do **not** rebuild it and do **not** flag it as an H2H gap. The three cards (`OVERALL RECORD`, `ON HARD`, `SETS RECORD`) flow straight into `FULL MATCH HISTORY`. Test: an `H2H TREND` heading or `.ah2h-trend` element anywhere is wrong.
- **Weather "How conditions affect play" cards (founder ruling 2026-08-01).** The three cards — Court pace, Serve advantage, Rally length — each render **one scalar in [0,1]**: the bar fill is the scalar's position and the verdict word is its band, both read from the *same* scalar so the word and the fill can never disagree. The scalar is derived from the forecast (temp / wind / humidity) + surface through the single `WX_IMPACT_MODEL` object; **its thresholds are ratified (founder 2026-08-01) — do not re-tune them without a fresh ruling.** Closed vocab only: Court pace *Faster / Neutral / Slower*; Serve advantage *Elevated / Neutral / Reduced* (never "Server"/"Returner" — those are the pole labels, not verdicts); Rally length *Longer / Neutral / Shorter*. Fill direction: a **fuller** bar = slower court / more server advantage / longer rallies. Colour is muted grey `#5b6880` — **never green/red** (a weather verdict is not a player comparison). Absent forecast → **em dash, no fill** (a missing temperature is not 0 °C). Test: verdict word and fill share one scalar; the word is in the closed set; the bar grows toward slower/server/longer; no green/red anywhere; an absent forecast is an em dash, never a fabricated 0.
- **Key Factors — Stennisfy Model card destination (founder ruling 2026-08-02).** The full-width **Stennisfy Model** card at the foot of the modal's **Key Factors** tab (`akModelBlock`) deep-links to the **top-level Stennisfy Model page for that match** — *"exactly to the stennisfy model."* There is **no** model tab inside the modal, so the card routes out via `openEdgeModelFromMatch(id)`, which **closes the analysis overlay first** (else the page switch hides behind it and reads as a dead click) then calls `openEdgeModel(id)`. The empty/unpriced state links too (the model page carries its own empty state). Card gets a button affordance (hover tint, focus ring, right-aligned `›` chevron). Test: the card is `role="button"`; clicking it closes the modal and lands on the `edge` page/nav for that exact match id — it does not stay static and does not route to Odds.
- **Key Factors — best-soft-book "vs fair" gap (founder ruling 2026-08-02).** In the Stennisfy Model card, the **Pinnacle** box shows its real `edgeVsPinnacle` "±pp vs fair" (same edge that drives the SHARP VALUE / NO VALUE flag). The **Best soft book** box shows **only the real price + bookmaker name; its "vs fair" line is em-dashed (`● —`)**, never a computed pp. Rationale (chosen as the most logical of "keep raw / em-dash"): best odds **can't be de-vigged** — the best price per side can come from different books, and even a single book's price still carries its overround — so `fairP − 1/softPx` would fold the book margin into the "edge", a systematically biased, apples-to-oranges number sitting next to Pinnacle's clean edge. Consistent with the standing "never show a false-precision number" rule. Test: exactly the two Pinnacle boxes carry "pp vs fair"; both soft-book boxes show `● —` while still printing the soft price and book name.
- **Key Factors — tournament tier in the title (founder ruling 2026-08-02).** The Tournament card title (`akTournamentBlock`, `.akt-title`) renders **"City · TIER"** (e.g. `Rotterdam · ATP 500`). The tier is **not** in the feed (`tourBadge` is only "ATP") and there is **no other source** — *"just fix it with what you have."* Derive it locally: prefer the per-draw **`m.venue.category`**, else a lookup in the static **`TOURNAMENT_CATALOG`** by cleaned event name. When neither knows the event, render the **name alone** — never a fabricated tier. Test: a catalogued/venue-tagged event shows `Name · TIER`; an unknown event shows the name with no `·` tier suffix.
- **Player Profile — export parity (founder ruling 2026-08-01).** The Player Profile page (`buildPlayerProfileHtml`) drops three intentional enrichments the export lacks. **(1) No "Surface record" rail card** — the Layer #4A career + rolling-52-week per-surface card is removed; the left rail carries only the **Surface performance** bars. **(2) Recent form is a flat, most-recent-first list** — one row per match, labelled inline with its tournament, **not** grouped under per-tournament headers (tournament grouping stays only in the modal Form tab). **(3) The header archetype line is a single archetype label** (e.g. `All-Court Elite`, or a composed `A / B`) with **no appended "· <surface> Specialist" tag**, coloured **primary text `#e7e9ee`** — never brand blue (Blue rule). These are the **fourth** export-has-not / product-had divergence resolved toward the export, alongside Extra-stats, the Summary view and the H2H trend. Test: the profile rail has no "Surface record" card; Recent form shows no tournament group headers; the archetype line is one label in `#e7e9ee`, not blue and with no Specialist suffix.

- **Records must match Flashscore — count qualifying matches (founder ruling 2026-08-04).** Every surface that shows a win/loss record computed from our api-tennis archive must follow the **same counting rules as Flashscore**: all decided professional singles at any level (ATP + Challenger + ITF) **and any round including qualifying**. Do **not** apply an `event_qualification`/main-draw gate on these surfaces — it made our numbers a strict subset of the public record (Buse 2024 read 36/22 vs Flashscore 47/28; the gap was entirely dropped qualifying + null-flagged rows). Applies to `buildAllTierYearly` (Record-by-season), its drill-down twin `playerMatchHistory`, `seasonSurfaceByTier` (current-season surface record), `recentFormFromFixtures` (which additionally tags qualifying rows `'Q'`), and `fetchPlayerCareerHistory` (Record-by-**tournament** — extended 2026-08-04 after the founder reinforced that *every* record/results surface must share the same Flashscore rules; qualifying rows are tagged `'Q'`, rank -1, so W/L totals grow but bestResult/titles are untouched). Scope note: provider-aggregate stats (career/surface win %) and Tennis-Abstract career splits are separate data sources and are *not* forced to this rule. Separate, unrelated cause: a player's Record-by-**tournament** can still read below the public record when the api-tennis archive is genuinely *missing* older editions (e.g. Zverev's pre-2023 Canada) — a data horizon, not this qualifying filter, and not closed by counting qualifying. Test: for any player on the board, the Record-by-season totals equal Flashscore's per-year Match Record, and their per-tournament W/L includes qualifying rounds; no qualifying/main-draw gate exists in any archive-record builder.
- **Break/Hold heatmap — window 24 months, axis = SIX single-ordinal rows, roster rule Option B, desaturation not a floor (founder GRID RULING 2026-09-02, TEN-107; supersedes the 2026-09-01 3-bucket ruling).** The `holdbreak.json` shard (built by `build-holdbreak.js` from the harvested historical point-by-point in `apitennis-holdbreak-cache/`) rolls up SERVE hold% and RETURN break% over a **24-month** window (not the app's 52-week default — a window does not transfer between measurements), on a **single-ordinal** position axis: **one row per the server's service-game ordinal within the set, `meta.buckets === ['1','2','3','4','5','6']`**, keyed on the *server's service-game ordinal within the set* (never the raw game number, which alternates server), with **ordinal 7+ folded into row `'6'`** (7+ = 2.03% of games, essentially all ordinal 7). The frontend labels the rows by their game-number range (`Game 1-2` … `Game 11-12+`). Columns are set 1/2/3/4/5; surfaces all/hard/clay/grass. hold = `serve_winner === player_served`, break = the complement (so per cell hold% + break% = 100). Source is pbp only — box scores and Tennis-Abstract splits have no game position and cannot build it. Service games are **tiebreak-fixed** (`setKey` returns null for any `/tie/i` set label — a tiebreak is one game both players serve, so its mini-serve rows are dropped, ~18.6% of the raw count). **Roster (Option B):** a player appears iff **numeric key ∧ (rank ≤ 400 OR ≥ 150 tiebreak-fixed service games in the 24M window)** — OR not AND, so a ranked player with thin data still qualifies on rank alone and a well-sampled unranked/low-ranked player qualifies on the count. Rank is read from the frozen `player-profiles.json` (July data; the rank clause can't yet drop fallen players — TEN-135). The shard emits `{pct, n, won}` on every cell and **never drops data** — there is **NO sample floor**; below-threshold cells are handled in the frontend by **desaturation** (colour muted, % + fraction unchanged), a display parameter (`HB_DESAT_N`, currently 20), not a floor and not a founder-locked threshold. GLOBAL (sum of S1..S5) is derived in the frontend and rendered **visually dominant** (wider, heavier, divider) because it survives at every rank tier while the per-set columns thin out for the tail. Test: `holdbreak.json` `meta.windowMonths === 24` and `meta.buckets === ['1','2','3','4','5','6']`; `meta.coverage.serviceGames` equals the sum of `pooled.serve.all[set][ord].n` (conservation); any cell where `serve.hold.pct + return.break.pct` for the same (surface,set,ordinal) ≠ 100 (±0.11) is wrong; any player key that is non-numeric, or rank > 400 with < 150 service games, must be ABSENT; a raw-game-number axis, a 3-bucket early/mid/late axis, a 52-week window, or an emitted sample-floor drop is wrong.
- **A pre-match walkover a player GAVE is NOT a loss (founder ruling 2026-08-04).** *"Yeah a pre-match withdrawal is NOT a loss."* When a player withdraws **before** a match and hands his opponent a walkover, that match counts as **neither a win nor a loss** on every W/L record surface (Flashscore convention, and consistent with Recent Form already showing `w/o` as neutral). The signal is the feed's `event_status === 'Walk Over'` **and** the profiled player being the recorded loser — key on that, **never** on the empty scoreline (a *retirement* the provider stored with no partial score would else be misread). Two distinct, opposite cases must stay correct: a walkover **RECEIVED** (he's the winner) is still a **win**; an **in-match `Retired`** (he quit mid-match) is still a **loss** for the retiree — only the pre-match walkover-*given* is excluded. Shared predicate `isWalkoverGiven(f, won)`; the aggregate record builders (`buildAllTierYearly`, its drill twin `playerMatchHistory`, `seasonSurfaceByTier`, `seasonRowFromFixtures`, `courtSpeedRecordFromFixtures`) **skip** the fixture; `fetchPlayerCareerHistory` (Record-by-**tournament**) **keeps** the row but tags `res:'WD'`, does not add to `lost`, and leaves `_won:false` so it can never become a title while its round still drives the edition finish badge (e.g. Djokovic Paris '11 stays "Quarter-final", 51-10 → 51-9; Wawrinka Rome '13 25-18 → 25-17). Frontend renders `WD` neutral grey with verb `w/o to` — never the red loss colour. Known cache lag: cached opponent profiles rebuild on the `PROFILE_SCHEMA_VERSION` bump (season/surface records) and record-by-tournament on the new `TOURNAMENT_HISTORY_SCHEMA_VERSION` guard, both throttled so corrections roll out over a few runs. **Not yet applied** (flagged to founder, separate decision): H2H record (`get_H2H` may not carry `event_status`, walkover meetings are rare) and the Recent-Form win-%/insights momentum metric. Test: a fixture with `event_status:'Walk Over'` where the player is the loser adds 0 to won and 0 to lost on every record surface; a `Retired` loss still adds 1 to lost; a `Walk Over` win still adds 1 to won; the per-tournament panel shows a neutral grey `W/O` row, never a red `L`.
- **A dead odds feed is a credits outage, not a product signal — do not gate controls on it (founder ruling TEN-179, 2026-09-09).** *"We will have soon the subscription of odds papi to pull all the odds, so leave it just enabled. It's just that the credits are out."* The oddspapi historical-odds capture (`odds-history.yml` → `/v4/historical-odds`) has been failing on HTTP 429 since 2026-07-25, so `openingOdds`/`closingOdds` are absent, the lazy `odds/{eventKey}.json` shards 404 and `odds-index.json` is `[]`. That is a **billing state with a known end date, not a data fact about tennis** — so the UI must not harden around it. Concretely: the Completed sort **`Biggest upset` stays ENABLED** even though `upsetScore()` returns `-1` for every match today and the sort therefore no-ops (line ~5777, third tuple element stays `false`). It is *not* given the `needs odds` note that `Biggest move` and `Market got it wrong` carry — those two were already disabled before this ruling and stay as they are; do not "make them consistent" in either direction. Test: `Biggest upset` is selectable in the Completed sort with no `needs odds` note; a future run that disables it on the grounds that it orders nothing has broken this ruling. **Corollary — the em-dash path is the steady state, not a bug to route around.** Per the Em-dash-vs-zero rule, absent opens render `—` in `#4b5672`; never derive an opening price from the current price, and never substitute another book (TEN-124 pins both legs to bet365). When the subscription lands, the two `finalScore ? (m.openingOdds?.… ) : null` gates (~line 6636/6637 and the `openOf` helper ~6743) must come off so an *upcoming* card can show a real open — tracked separately, not in TEN-179.
- **Merged Matches page — one page, two modes; the Upcoming rail is `yesterday · today · +1 · +2` (founder rulings TEN-179, 2026-09-09).** Matches and Results are **one page in two modes** (`state.view`), a segmented `Upcoming | Completed` control immediately left of the date rail's back chevron, and **one** card component in two states — never two card components. Mode switches exactly four things: page header copy, date-rail scope, summary boxes, card contents. **Rail:** four items each; **Upcoming is `yesterday · today · +1 · +2`**, Completed is today-backward (`daymin3 · daymin2 · yesterday · today`); switching modes resets to today; chevrons step one day with no scroll or animation and disable at each end; the rail greys to **45%** and goes `pointer-events:none` while search is non-empty. **The export's "today forward" is OVERRIDDEN and the loose end of the earlier gate is closed (founder ruling, gate `96dff8c9`, 2026-09-09):** *"I want yesterday back — yesterday / today / +1 / +2, as the 2026-08-08 rail."* He was asked because his two answers in the first gate read opposite ways — q2 chose "keep four tabs as specced", q3 left "no yesterday tab" unticked — and **this later, direct answer governs both**. Reason: api-tennis publishes only ~1 day ahead, so the forward rail's `+2`/`+3` tabs were permanently empty while a real day of play sat one step off the rail. Shipped in `1dd863a`; the `day3` (+3) tab is **retired — hidden in both modes** (its bucket and label survive unrendered so the slot can be re-enabled by unhiding the tab). Do not re-derive the forward rail from `SPEC.md` — the export is superseded here. Test: in Upcoming the visible day buttons are exactly `yesterday, today, tomorrow, day2`, the back chevron is disabled on the yesterday tab and the forward chevron on the +2 tab, and `#day3Tab` is not visible in either mode. **The day/view rule is now SYMMETRIC — a past day inside Upcoming renders as Completed (founder ruling, gate `e08a0bce`, 2026-09-10; shipped `21c6f86`).** This **supersedes** the previous "known and accepted: the Upcoming `yesterday` tab renders 0 cards" clause — he chose *"Mirror it — a past day picked in Upcoming shows that day's settled cards"*, so an empty Yesterday tab is now a **bug, not accepted behaviour**. The rule in one sentence: **on a day that can only hold one kind of match, the view filter follows the day, not the toggle.** Both directions live in one `effView` expression in `getFiltered()`: a future day (`tomorrow`/`day2`/`day3`) inside Completed renders as Upcoming, and a past day (`yesterday`/`daymin2`/`daymin3`) inside Upcoming renders as Completed. **A LIVE match is exempt from the past-day half** (`&& !m.live`) — the standing rule of this filter is that live is never dropped for being past (same carve-out as the `state.day === 'all'` branch), and without the exemption an in-play match carried over from a past day would vanish from *both* modes, since `isFinishedMatch()` is `!m.live && !!m.finalScore` and Completed already rejects it. Do not "simplify" the exemption away. Test: with yesterday holding only settled matches, Upcoming→yesterday and Completed→yesterday render the **same card count with the same settled layout** (`.mc-sets` present), while Upcoming→today and Upcoming→tomorrow are unchanged and the Completed rail's counts are untouched. **Known and accepted (unchanged by this ruling):** the page header and summary strip are **mode-scoped, not day-scoped**, so Upcoming→yesterday shows the header `Today's Matches` and the Upcoming summary trio over a settled day. That asymmetry is pre-existing — Upcoming→`+1` already carried the same header before this change — and is the mirror of the accepted forward carve-out (Completed header over upcoming cards); it is not a regression from `21c6f86` and must not be "fixed" without a ruling. **Settled indicator:** it is **back** in the Completed header (founder confirmed 2026-09-09, reversing his own §9 of 2026-08-08, which had removed it partly because it "isn't in the export" — it *is* in the merged-Matches export). **Sort drives the card:** Upcoming sort is `Time | Drift` and the card's right-hand block reads `state.sort` through one predicate (`mxDriftView()`) — never a second flag, so they cannot desync. Time shows `ODDS · IMPLIED` + `RECENT FORM`; Drift shows `OPEN · NOW`. Clicking the `BIGGEST MARKET MOVE` summary box sets sort to Drift and moves the sort control's label. **Routing:** the `Results` sidebar item is **removed**, `#Results` still deep-links into Completed, `Matches` stays highlighted in both modes, toggling the mode rewrites the hash, and the hash is applied at the **top** of `loadMatches()` with `{defer:true}` (state only, no render) — applying it at the end makes a `#Results` visitor watch the Upcoming board for the whole data load before it flips. Test: one card component; the rail is four items and Upcoming's first tab is **yesterday**; the Completed header carries the settled indicator; changing the Upcoming sort changes the card's right block with no second flag involved; a `Results` sidebar item anywhere is wrong while `#Results` must still land in Completed.
- **Completed cards are TWO-UP with their own 525px minimum; the two-up floor is a 1378px viewport (BISECTED) and the longest names WRAP rather than truncate (founder rulings TEN-179, gates `9970a72a` + `439b9cbe`, 2026-09-09/10).** The Matches grid is `repeat(auto-fill, minmax(430px, 1fr))`, and **Completed overrides it to `minmax(525px, 1fr)`** via `#matchlist:has(.match-card.cmpl)` — a Completed row is genuinely wider than an Upcoming one, so it carries its **own** minimum rather than forcing one number on both. Founder's instruction: *"the 430px card minimum in TOKENS is the thing to change, not the layout."* **Budget, MEASURED off the rendered DOM at a 1400px viewport** (list 1084px → two 536px cards → 502px of row), worst realistic case = longest name on tour + FAV chip + fully populated journey: `name 230.6 | sets 84 (best-of-3) | spacer 16 | hairline 1 | journey 169 = 500.6px`, **1.4px spare** (the spacer was 4px and the spare 13.4px until the 2026-09-10 score-cluster-clearance ruling in the entry below took 12px of it). Before the re-cut the same row needed **579px and spilled +77px past the card edge**. Do not re-derive these five column widths from `TOKENS.md` — the export's own row (`200 | sets | 218`) does **not** fit the export's own card minimum, which is the bug this clause fixes. **`overflow:hidden` is REMOVED from `.match-card`** (now `overflow:visible`) on the founder's explicit instruction: *"Silent clipping that swallows a closing price is exactly the failure mode we can't have — if content ever spills again I need to see it, not have it disappear."* Corner rounding is preserved by rounding the first/last child instead. **Two consequences the founder ruled on directly (gate `439b9cbe`, both answered "keep as shipped"):** **(1) Best-of-five does not fit** — its set cluster costs ~40px more, putting the longest names **26.6px over budget** at a 536px card. The shipped answer *was* that Completed names **WRAP to a second line** — **SUPERSEDED 2026-09-10 by the score-column-alignment ruling in the next entry: the name now truncates with an ellipsis inside a fixed column.** The wrap only made sense while column 1 sized to its own content; against a name-independent column a wrap produces a two-line row and still cannot move the score block, so it buys nothing. The two measured alternatives were **rejected**: tightening the score gap 4→2px and drift lane 28→22px (closes to 1.8px, still short, and over-tightens the score group), and two-up only above ~1550px (fits on one line but gives most laptops one column). **(2) The collapse point is 1378px, NOT the ~1100px originally asked for** — two Completed columns need `2 × 525 + 12` = **1062px of list width**, and `#matchlist` is inset **316px** from the viewport (250px sidebar + page padding), so two-up engages at **1062 + 316 = 1378px**. **MEASURED BY BISECTION off the live DOM, 2026-09-10: 1378px renders two columns, 1377px renders one** (list width 1062px vs 1061px). At 1200px a card would be 436px while the row needs 488, so two-up there would spill 52px on every Completed card. The founder accepted **the measured floor as the honest one** (gate `439b9cbe`: “Accept ~1358px as the honest two-up floor”); between 1100 and 1378 the page is single column, which is the fallback he specified, reached earlier than he expected. A future run that "restores" the 1100px number has broken this ruling. **CORRECTION OF RECORD:** he was told this floor was `~1358px` and ratified it under that number. `1358` was arithmetic from a wrong 296px inset, never a measurement; the real inset is 316px. The decision he made — accept the measured floor rather than force 1100px — is unaffected, but **1358 is wrong; do not re-introduce it.** **Two pre-existing bugs this work surfaced, both already live before it:** the fixed **200px name column was already ellipsising** `B. Van De Zandschulp` (needs 230.6px) on the shipped **single-column** build at every width, and the **journey was clipping itself** (234px of content in a 218px track) — invisible only because it renders empty while both odds legs are null. **Retired matches render their partial sets** (founder reversal of his own TEN-8 drop-the-score rule): the scoreline shows, and the set count is **recomputed via `mcSetIsWon()`** rather than trusting the feed, which credits the **abandoned** set (Khachanov v Blockx `6-2, 7-5, 3-2` → tile reads **2 – 0**, not the feed's 3 – 0). The test is whether a set actually *finished*, **not** "more games wins the set" — 3-2 has more games and is not a set. Verified to reproduce the feed exactly on all 7 ordinary completed matches and differ on **only** the one retirement, so it can never reach a normal scoreline. The **RETIRED chip moved to the card header** because at 62px in the row it pushed even a short name onto a second line; it has 3.1px of room there, so a tournament label longer than `ATP US Open · Grand Slam` on a retired card pushes the OPEN/CLOSE caps to a second row — visible degradation, not clipping. **Also corrected in passing:** the OPEN/CLOSE caps were right-anchoring over the **delta** instead of the closing price (a flex row with a growing spacer); the block is now a grid mirroring the journey's tracks, as `TOKENS.md` asks. Test: `#matchlist:has(.match-card.cmpl)` uses `minmax(525px, 1fr)` while the base grid stays `minmax(430px, 1fr)`; `.match-card` is `overflow:visible`; ~~`.mc-players.cmpl .mc-name` does **not** set `text-overflow:ellipsis` or `white-space:nowrap`~~ **(that clause is DEAD — superseded by the next entry, which requires exactly those two properties)**; a retired fixture renders its partial sets with a set count recomputed by `mcSetIsWon()` and not the feed's own; the RETIRED chip is in the card header, not the player row.
- **Completed set scores live in FIXED columns, allocated to the BOARD's highest set count, and the name column is name-independent (founder ruling TEN-179, 2026-09-10; shipped `c78b0e8`).** Founder's brief: *"Set scores currently sit flush against the player name, so their horizontal position shifts with name length… Flashscore is the reference and it aligns every set into a fixed column."* **Root cause — each player row is its OWN grid**, and column 1 was `minmax(0,max-content)`, which sizes to *that row's* name. MEASURED on the live Mon 7 board before the fix: within a single card, set 1 of the top row sat **36.1px** (Shelton/Tsitsipas) and **82px** (Gea/Van De Zandschulp) from set 1 of the row below. **The fix is that column 1 must never depend on the name**: it is `minmax(0,260px)` — a cap, not a floor, so it shrinks at a 536px two-up card and opens to 260px one-up, but it resolves **identically for both rows of a card and for every card on the board** because its sizing has no content input. **The name therefore TRUNCATES with an ellipsis** (`white-space:nowrap; overflow:hidden; text-overflow:ellipsis; min-width:0`) — this **REVERSES gate `439b9cbe`'s "keep the wrap"** on the founder's explicit instruction, and the previous entry's "does not set ellipsis/nowrap" test is dead. `min-width:0` is load-bearing: `.mc-id` is a flex row and without it the name refuses to shrink and pushes the FAV chip out of the track instead. **Set cells are allocated to the highest set count on the BOARD being rendered, not the card's own** (`boardSetCols`, reduced over `filtered` in `renderMatches`, threaded through `mcSetCluster(fs, who, recount, cols)` and `mcTermCell(m, who, cols)`) — a per-card allocation would satisfy the words but not the test the founder actually stated, *"a 3-set match doesn't shift its columns relative to a 5-set one on the same board."* On an all-best-of-3 day the allocation is 3 and no width is wasted. **Unplayed sets render BLANK, never a dash** — the standing rule is that a dash means *missing data*, and sets 4–5 of a straight-sets win were simply not played. **Set-count tile keeps its own cell with a 14px gutter to set 1, against 8px between sets.** This narrows — it does **not** revoke — the 2026-09-10 item-4 ruling that the group gap is a uniform 8px read off the design export: 8px still governs set-to-set, and only the tile→set-1 gutter widened, because the founder pointed at Flashscore's separation of the bold total from the greyed set scores. Implemented by making grid track 1 wider than the 24px chip that sits in it (`calc(--mc-tile + --mc-set-gutter - --mc-set-gap)`, chip `width:24px; justify-self:start`), so no bespoke margin can shrink the chip inside its own track. **Per-set cell stays 16px**: a two-digit game (`13`) measures **15.61px** in Mono 13px, so 16px is the floor at which single- and double-digit values still share a centre. **Tiebreak superscripts are `width:0`** (`display:inline-block; overflow:visible`) so they are outside the cell's width calculation entirely — the cell is a fixed track and could never widen, but the *other* half of the failure is real: on the old build an in-flow superscript made `7⁷` centre as a pair and pulled its game digit **3.2px left** of a plain `7` in the row below. The old `top:-0.6em` on that `sup` was dead code (statically positioned); the raise is `vertical-align`. **MEASURED after the fix, live data, 1400px:** all 8 rows of the Mon 7 board place their set cells at card-relative x **221 / 245 / 269 / 293 / 317** (one distinct tuple across 3-set, 4-set and 5-set cards) and their digit centres at **229 / 253 / 277 / 301 / 325**, identical for plain, tiebreak and double-digit values; **230 geometry assertions recomputed from the token arithmetic plus raw `matches.json` produced 0 mismatches**; the same probe run against `e8deb86` reproduces every defect, so it is a real detector and not a tautology. **Known and accepted:** the name track is what is left after the score block and the 169px journey — **202px on an all-3-set board, 154px when the board holds a 5-setter** (214/166 until the score-cluster-clearance ruling below) — and the FAV chip costs **42.2px** of it, so truncation is board- and FAV-dependent (`B. Van De Zandschulp` renders in full on a 3-set board and truncates on a 5-set one). **A walkover is the one Completed row still sized by its own content** (`mcTermCell` returns an empty cluster when there is no sets array at all), so a walkover card's name column comes out wider than its neighbours'; left deliberately, as `.mc-sets--term` is `display:flex` and re-laying it out buys nothing visible. Tests: `.mc-players.cmpl .mc-row`'s first track is `minmax(0,260px)` and contains no `max-content`; `.mc-players.cmpl .mc-name` **does** set `white-space:nowrap`, `overflow:hidden`, `text-overflow:ellipsis` and `min-width:0`; every `.mc-sets__g` on a board reports the same width and the same card-relative x as its opposite number in the other player row; a card with fewer sets than the board maximum emits trailing **empty** `.mc-sets__g` cells and never a dash; a `.mc-sets__g sup` computes to `width: 0px`; the Upcoming row grid is untouched at `228px 130px 120px` with zero `.mc-sets` present.
- **The score cluster clears the hairline by 16px, and that gutter is paid for by the NAME track 1:1 (founder ruling TEN-179, 2026-09-10).** Founder's brief: *"On a 5-set card the last set cell sits too close to the vertical hairline… `7⁷` nearly touches the divider. Move the cluster's start x left enough that the final cell on a 5-set board clears the hairline with a proper gutter."* **`.mc-players.cmpl .mc-row` col 3 goes `minmax(4px,1fr)` → `minmax(16px,1fr)`.** At a 536px two-up card on a 5-set board the `1fr` is FULLY SQUEEZED, so col 3 resolves to exactly its minimum and **that minimum IS the clearance**. **THE COST IS UNAVOIDABLE AND THE FOUNDER'S STATED EXPECTATION WAS THE OPPOSITE** — he wrote *"the name track gains width from this"*; it **loses** width, 1:1. Col 1 is `minmax(0,260px)`, a cap that absorbs ALL row slack, so every px freed anywhere else in the row flows into the NAME and the cluster does not move; widening col 3 is the **only** lever that moves the cluster left, and it comes straight out of col 1. Do not "optimise" this by trimming the 12px name→score gutter, the 14px tile→set-1 gutter or the 8px set gap expecting the clearance to grow — it will not; the name track will simply absorb the saving. **MEASURED by runtime A/B on the live deployed bytes at 1400px, every completed board, gutter → (clusterX, nameTrack, new truncations):** `4 → (195, 166, —)` · `12 → (187, 158, —)` · `16 → (183, 154, —)` · `18 → (181, 152, C. Alcaraz)` · `19 → (180, 151, + B. Shelton)` · `24 → (175, 146, + A. Blockx)`. **16px is chosen, not estimated:** it reproduces the design export's guaranteed **15.5px** minimum name→score gutter on the 4px token grid, and it sits 1px inside the **BISECTED** no-new-truncation boundary of **17px**. **This DEVIATES from `TOKENS.md`**, which specifies `minmax(4px,1fr)`; the founder's instruction overrides the token, and a future run that "restores" 4px from TOKENS has broken this ruling. **Accepted cost, reported not hidden:** on a 5-set board `K. Khachanov` clips **10.5px → 22.5px** and `B. Van De Zandschulp` **64.6px → 76.6px**; both already truncated before the change, and no NEW name truncates. **MEASURED after the change on the patched build against live data:** set cells at card-relative **183 / 221 / 245 / 269 / 293 / 317** — ONE tuple across all 8 rows of the Mon 7 board — widths `24/16/16/16/16/16`, tile→set-1 **14px**, set→set **8px**, clearance **16px** and divider x **349** on every board (Mon 7 / Tue 8 / Wed 9 / Today), so the journey column did not move; **zero row/card/document overflow at 1377 / 1378 / 1400 / 1600px** and the two-up floor is still **1378px** (1377 → 1 column), measured against the unpatched live build as a control. Tests: `.mc-players.cmpl .mc-row`'s third track is `minmax(16px,1fr)`; every `.mc-sets > *` on a board reports one card-relative x tuple; the gap between the last set cell's right edge and `.mc-vdiv`'s left edge is 16px on every completed row; no row child's right edge exceeds its card's inner edge at 1378px. **16px is now LOCKED, and the gutter is CLOSED as a lever (founder ruling TEN-179, gate `2cc0c95a`, 2026-09-10).** Offered keep-16 / go-17 / go-20-and-accept-truncation / neither, he chose **neither**: *"revisit by moving the FAV chip out of the name cell instead (frees 42.2px, separate task)"* — split out as **TEN-182**. So the clearance stays at 16px until the FAV chip stops consuming name width; a future run must **not** widen col 3 past 16px to buy air, because the price is name truncation he has explicitly declined to pay. Test: `.mc-players.cmpl .mc-row`'s third track is `minmax(16px,1fr)` and stays there while `.mc-fav-tag` is still a child of `.mc-id`.
- **Authoritative fields beat heuristics — a set-count net may never override a main-draw signal (founder ruling TEN-157 / TEN-161, 2026-09-06).** A match's round / qualifying label is derived by a **strict signal hierarchy, highest wins**: **(1) `event_qualification`** (the feed's `'True'`/`'False'`/`null` *string* — see `bsp-pipeline.js`) and **(2) the round label itself** — the `1/N-finals` fraction form (`1/32-finals`, `1/16-finals`, `1/8-finals`, …) or its word form (`Round of 64`, `Quarter-finals`) — are **authoritative**. A **set-count net** — any heuristic that infers "qualifying" from a best-of-3-vs-best-of-5 set count (the Slam-qualifying-leak class of detector) — is a **last-resort *tertiary* signal**: it may only *fill* a label when BOTH authoritative signals are absent or genuinely ambiguous, and it may **never override an authoritative main-draw signal.** A fixture whose `event_qualification !== 'True'` carrying a main-draw round label is main draw, full stop, however few sets it went. This is the TEN-157 bug: a US Open **second-round main-draw** win (R64 / `1/32-finals`, `event_qualification: 'False'`) was stamped **`'Q'`** because the set-count net fired on a short/straight-sets result and outranked the authoritative fields. **Guarded by the P3 regression harness (TEN-160)**, which encodes the ruling as cases: in-play 2-1 main-draw R64 → not `Q`; settled 3-1 → R64; genuine qualifying row (`event_qualification === 'True'`) → `Q`; Slam retirement at 2 sets → main draw. Test: no code path assigns a qualifying / `'Q'` label to a fixture whose `event_qualification` is not `'True'` while a main-draw round label is present; the set-count net is reachable only after both authoritative signals are missing; a main-draw match that ended in 2 or 3 sets (retirement, walkover, or straight sets) keeps its main-draw round, never `'Q'`.
- **Odds-data honesty — four standing rules (founder, TEN-179, 2026-09-10).** These bind every odds surface, not just the Matches page. (1) **Never fabricate or approximate: missing data is a dash** — never a zero, never a plausible default, and above all **never an opening price derived from the current price**. (2) **Every drift figure is labelled for what it is** — `Bet365 open → close` once settled, `Bet365 open → now` while unsettled — **not "market move"**. The second column on an Upcoming card is `NOW`, on a Completed card `CLOSE`; that distinction is load-bearing and must not be flattened. (3) **Thin coverage is flagged with the count, not filled** — a figure computed over a subset states its denominator (`mcOpenCoverage()` renders "1 of 2 matches with an opening price"). A partial board must never render as a whole one. (4) **Only fixture discovery may consume oddspapi quota.** `/v4/historical-odds` and `/v4/account` are free; the single `/v4/fixtures` lookup is the entire billable spend, and `report_consumption()` prints the MEASURED `/v4/account` delta every run and warns above one unit. Tests: no code path assigns an opening price from `bestOdds`/`odds`; every drift caption names bet365 and both legs; a summary panel computed over a filtered subset renders a count; a capture run's meter delta is 1.
- **The Completed subtitle KEEPS its opening-to-closing claim, and the CLOSE is the last stored pre-start bet365 price (founder ruling TEN-179, gate `d22d7ac9`, 2026-09-10).** Asked whether the Results subtitle — *"Every settled ATP match with its opening-to-closing odds journey."* — should be retracted to *"Every settled ATP match with its closing price"* because the page could not deliver a journey, the founder ruled the other way: *"Since you have access to a paid version of oddspapi and there are 5000 requests a month, we should be able to have the opening and closing odd. If you don't get the closing odd, you can take the last possible request just before the start of the match."* **The subtitle is therefore NOT changed** — the fix is to deliver the data, not to weaken the claim. A future run that "corrects" this subtitle to a closing-price-only wording has broken this ruling. **The close is defined as the last price bet365 stored at or before the fixture's start time** — never a live in-play price, never a post-start price, and never derived from the current price (which remains the standing TEN-124 pin: `_mcCloseOf` reads `closingOdds` only, no `bestOdds`/`odds` fallback). **Both legs come from `/v4/historical-odds`, bet365 only** — the endpoint returns bet365's own timestamped series (market `121`, outcomes `121`/`122`) which oddspapi records whether or not we poll, so **one call made after the match starts yields both the open and the close, and that endpoint is free** (it does not increment the request counter; only fixture discovery is billable). No hourly polling or pre-start burst is needed. **MEASURED 2026-09-10** across all 8 settled matches on the live board: real open **8/8**, open lead-time median **36.3h** before start, last stored pre-start price median **7 min** before start with **8/8 inside 15 min**, 158–602 points per fixture; one `/v4/fixtures` call returned **1,395 fixtures for 1 quota unit**. **Never blend books** (founder, gate `9970a72a` q2): *"A Pinnacle open against a Bet365 close is a margin gap, not a market move"* — which kills `pinnacleOpen` as an OPEN-cell fallback for good. **The single defect blocking all of it:** a `/v4/historical-odds` batch containing **any** book the plan does not entitle returns **HTTP 403 for the whole batch, bet365 included**, so `refresh-odds-history.py`'s `BOOKS = ('pinnacle','williamhill','1xbet','betsson','betano','bet365')` at `BOOK_BATCH = 3` sends two batches, both 403, and the capture writes nothing **while reporting success**; it must be `BOOKS = ('bet365',)` (same defect in `refresh-odds.py`, where the dead books are billable). Display side is already proven: with the real measured prices injected into the live bytes, all 4 journeys render (em-dash path count 0), the journey occupies exactly its **169px** track sitting **17px inside** the card edge with no page overflow, the `OPEN`/`CLOSE` caps sit **pixel-exact** over their figures (both span 636–668px / 706–746px at 1400px), and the longest name on tour plus a full journey still renders **one line, no ellipsis, 19.6px spare**. Test: the Completed subtitle still reads "opening-to-closing odds journey"; no capture script requests a bookmaker outside the active plan's entitlement; a `closingOdds` value never originates from a price timestamped after the fixture's start.
- **The bet365 history archive is per-month shards in REDUCED form, backfilled across the whole window, and a fixture is NEVER re-pulled (founder ruling TEN-179, gate `2334e509`, 2026-09-10).** Three answers, all executed in `archive-bet365-history.py` + `.github/workflows/bet365-archive.yml`. **(q1 → structure B, reduced):** `bet365-history/YYYY-MM.json`, one shard per month, each fixture stored as **open + close + 22 interior points** (`KEEP_POINTS = 24` per player). The full series was rejected at 15–20× the size for points three minutes apart that mostly repeat. **(q2 → full backfill):** the whole ~180-day retention window, accepting that everything older than ~4 weeks returns as thin residue — *"the residue is still a real open and close."* **(q3 → keep the export heading):** the summary box stays headed `BIGGEST MARKET MOVE`; the attribution line under the figure carries the `Bet365 open → now` labelling, and the heading is **not** renamed. A future run that renames that box to satisfy the drift-labelling rule has broken this ruling. **This dataset is ADDITIVE and touches nothing:** not `odds-archive/*.csv` (the Pinnacle 2010–2025 set on the Database page), not the `odds/{eventKey}.json` shards, not `matches.json` (owned by `refresh-odds-history.py`). **Never re-pull an archived fixture.** MEASURED 2026-09-10 across 90 fixtures / 6 windows / 3 tiers: oddspapi keeps the dense ~3-min series only ~4 weeks then prunes to an open/close residue, a **~20× drop** in stored bytes — the dense copy of an older fixture is **already gone on their side and cannot be recovered**, so a re-pull can only return a *worse* copy than the one we hold. The shards are therefore the checkpoint (`known_ids()` reads them back off disk); a fixture that is archived **or** recorded as a miss is never fetched again, and there is no side-car state file that could drift from what is actually stored. A **transport failure is not a miss** — recording it as one would permanently skip a fixture we merely failed to reach, so it is left unresolved for a later run. **The binding constraint is PACING, not quota.** `/v4/historical-odds` is free and **its 429s do not bill either** (re-verified: meter `100 → 100` across ~20 calls including several rate-limit rejections); only `/v4/fixtures` discovery is metered, ~1–2 units nightly and ~30 for a full sweep. But measured the same day: **there is no market filter** (`markets=` / `marketIds=` / `market=` are all ignored and the full ~1–4.5 MB, 119-market payload ships every time), the limit is **~1 successful call per ~5s**, and **concurrency does not help** — 4 parallel calls returned one 200 and three 429s. Do not "optimise" this with a thread pool. The full scope is therefore ~20+ hours of pacing, which is why the backfill is resumable and time-budgeted and drains over successive nightly runs rather than chaining self-dispatches. **The close is pinned pre-match here too:** `build_entry()` keeps only points at or before the fixture's real UTC start (`trueStartTime`, else `startTime`), so an in-play price can never become a close — the same TEN-124 pin the tick-cadence proxy violated on Zverev–Darderi. **Reduction never invents a point:** every stored point is a real observed one, sampled evenly by *index* over the compacted series (compaction has already stripped flat runs, so index-even keeps the movement structure while time-even would over-sample quiet stretches). `n1`/`n2` record the true pre-reduction point counts so thin coverage stays visible as a count rather than being filled — the standing "flag the denominator" rule. Timestamps are stored as **epoch seconds**, and the archive is published to Pages but is **deliberately on no startup path** (nothing fetches its `index.json` at boot), so it cannot regress the payload budget `matches.json` already dominates. Tests: `test-archive-bet365-history.py` (40 assertions, run as a fail-closed gate in the workflow *before* any network call) locks that reduction preserves the exact open and close, stores only real observed points, and never keeps a point after the first ball; `archive-bet365-history.py verify` re-reads every shard from disk and fails the run on a point cap breach, out-of-order timestamps, an in-play close, or an impossible price ≤ 1. A `BOOKS`-style widening beyond `bet365` is still forbidden — a batch with any non-entitled book 403s in full.
- **Capture cadence is OWNED BY AN INTERNAL LOOP, not by cron; OPEN is our own first captured bet365 price (founder ruling TEN-179, 2026-09-11).** *"One job looping internally for its hour instead of one cron delivery per capture. 27% cron delivery is the binding constraint and more cron entries won't fix it."* **MEASURED 2026-09-11: `odds-now.yml` at `0 * * * *` delivered 3 of 11 slots = 27% (gaps 2.6-2.7h); `odds-history.yml` at `0 */3 * * *` delivered ~5/day against 8.** That shortfall — not the write path, not the pipeline's `observedAt`/`capturedAt` guard, both of which were healthy — is what left a NOW price **76 minutes** behind a bet365 move. **Never quote a capture latency derived from a cron expression; quote the measured delivered rate.** A shorter cron makes it worse (GitHub sheds short-interval schedules first). `odds-now.yml` is now a supervisor tick that starts `odds-capture-loop.sh`, which captures on its OWN clock for ~5h30m: the FREE `/v4/historical-odds` first-appearance sweep every **15 min** (zero quota — `refresh-odds-history.py --first-appearance` reads the meter either side and `::error::`s on any delta), and the METERED `refresh-odds.py` NOW leg every **60 min**, which is DELIBERATELY UNCHANGED (flat hourly is the approved option (a); the loop exists to make hourly real, not to spend more). **Every iteration commits and pushes before it sleeps** (`ci-commit-push.sh`), so a kill — redeploy, timeout, cancelled run — costs at most the one iteration in flight; SIGTERM is trapped and flushes, and the sleep is `sleep & wait` because bash defers a trapped signal until a foreground command returns. On a push race with `scores.yml` the helper **aborts the rebase, resets to origin/main and returns REDO** rather than leaving a half-applied rebase that poisons every later iteration; only the FREE sweep is recomputed (re-running the metered one would spend quota to fix a git problem). **The delivered cadence is reported from `odds-capture-cadence.json`, not from the cron expression** — rolling 400 ticks with median/max gap. **CORRECTED 2026-09-11 (`14302fd`): a replayed iteration is recorded as `captured`, not `redo`.** The original rule excluded it to stop it *flattering* the median and did the exact opposite: `ci-commit-push.sh`'s REDO does `git reset --hard`, which reverts the cadence file too, so the raced iteration contributes **no** tick at all and excluding the replay as well made a slot in which a capture genuinely happened read as a 30-minute gap instead of 15. The gap population is now an **allow-list** of `outcome == 'captured'`, not a deny-list of `redo` — a deny-list counted any new outcome value as a capture, so a stood-down free leg reported a healthy 15.0-min median while zero captures were happening. **OPEN/NOW semantics (founder, same ruling):** OPEN = the first bet365 price WE capture, pinned once, never recomputed, never replaced by a later reading, **never oddspapi's ingestion point, never derived, never the series low**; NOW = bet365's current price and it must actually move when the market moves; the fix for OPEN == NOW on most cards is **always to make NOW fresher, never to adjust OPEN**. Every drift figure is labelled `Bet365 open → now`. Tests: `odds-now.yml` contains no capture-timing cron beyond the supervisor tick; `--first-appearance` never calls `/v4/fixtures`; the loop commits per iteration rather than at job end; the gap population counts `outcome == 'captured'` and nothing else, and a replayed iteration IS counted (its `mode` carries `+redo` for the audit trail).
- **OPEN is pinned to OUR FIRST SIGHTING, forward-only — never `series[0]`, never an ingestion instant (founder ruling TEN-179, gate `a0a2446b`, 2026-09-11).** *"OPEN is the last price Bet365 was actually showing when we first looked. Applies to fixtures from the point `firstSeenAt` exists. The already-published opens stay as they are — I'm not retroactively changing numbers we can't verify either way."* `series[0]` is the earliest point of whatever history oddspapi hands back, i.e. **oddspapi's ingestion instant**. One-line proof, re-runnable on today's board: Zverev `openAt 2026-09-10T13:41:00.351Z` and Tiafoe `openAt 2026-09-10T13:41:00.855Z` — unrelated fixtures, **504 ms apart**; bet365 did not open two markets in the same half-second. `bsp-pipeline.js` therefore pins the open to **`lastAtOrBefore(series, firstSeenAt)`**, where `firstSeenAt` is stamped prospectively and frozen by `open_monitor()` (`refresh-odds-history.py`, `setdefault` — never rewritten, which is what makes it a measurement rather than something reshapeable at pin time) into `odds-open-monitor.json`. The pin carries `src:'first-sighting'` and **`seenAt`**, so the gap between the price instant (`at`) and our sighting (`seenAt`) stays auditable off the published board alone — that gap is the open question the founder left open (see the dash-threshold note below). **FORWARD-ONLY falls out of the existing carry-forward**: a match that already carries an `openingOdds` never reaches the derive branch, so the published opens are untouched by construction — do not add a "re-pin existing" path. **Fallbacks are counted and reported, never silent:** no `firstSeenAt` → `series[0]`, counted `openNoSighting`; sighting known but no quote at/before it → same, counted `openSightingNoQuote`. **SUPERSEDED 2026-09-11 on the LABEL only (see the provenance ruling below): the fallback now carries `src:'ingestion'` rather than no `src`.** It still must not be dressed up as a sighting — `seenAt` is written on the `first-sighting` path alone. **The bet365-archive swap is GATED off a `first-sighting` pin** — the archive stores the same `/v4/historical-odds` series, so its first point is *also* an ingestion instant, and letting it win would reintroduce through the back door exactly what this ruling forbids; for a pre-monitor fixture the earlier 2026-09-10 "genuinely earliest" ruling still applies unchanged. **Honest, not earlier:** a sighting-anchored pin is by construction *later* than `series[0]` whenever the two differ, and that is the intended trade. **No dash threshold has been set** — the founder explicitly reserved that call; until he rules, a large `at`→`seenAt` gap publishes with the gap on the record rather than dashing. Tests: a fixture whose market moved between ingestion and our first sighting pins the LATER quote; a quiet market pins the same value as `series[0]` but still carries `src:'first-sighting'`; an archive open earlier than a `first-sighting` pin does NOT replace it; a pre-monitor fixture keeps `series[0]` and is labelled `src:'ingestion'` (NOT `first-sighting`, and with no `seenAt`).
- **Stale-NOW is measured at 120 minutes against the PUBLISHED board, on `observedAt`, excluding fixtures with no bet365 line (founder ruling TEN-179, 2026-09-11).** *"90 fires on a slipped pipeline cycle with nothing wrong, 180 sleeps through the exact incident that started this. Exclude fixtures with no Bet365 line; that's the open-monitor's job and conflating them gives me one alarm for two conditions."* `check-now-staleness.py`, run every loop tick. **Three non-negotiables, each naming the failure it prevents:** (1) it fetches the **artefact Pages is serving**, cache-busted, *not* the committed `matches.json` — capture staleness and *publish* staleness are different stages and the visitor sees the sum (proven 2026-09-11: tick 1 wrote Tiafoe at 3.4/1.333 while the published card still read 3.5/1.3, because the pipeline republishes from the committed file one cycle later, so a working-tree check calls a visibly flat card healthy); (2) it ages **`observedAt`** — when WE last looked — and **never `at`**, which is when bet365 last MOVED the price, so a market quiet for 20h would otherwise read as a dead pipeline four times an hour. This is why `bsp-pipeline.js` stamps `observedAt` on the **series** NOW path too (`src:'series'`, from `oddsMovement.capturedAt`): sharding strips `oddsMovement` before the board ships, so the capture clock must travel on `bet365Now` or it never reaches the published artefact at all; (3) a fixture with **no bet365 line is excluded** — that is unopened, which is the open-monitor's T-24h condition, and two conditions stay two alarms. **Exit code is always 0**: the alarm is the `::error::` annotation plus the record in `odds-now-staleness.json`, because a workflow that goes red four times an hour is how a real signal gets ignored (37 unread red runs, Sep 2-10). The threshold is env-overridable via `STALE_NOW_MIN` so **TEN-187 can re-derive it against a day of real loop cadence without editing code**. Known and accepted: a monitor inside the loop cannot report on a loop that is not running — that case is caught one delivered cron slot later, when the supervisor restarts the loop and its first tick measures an unrefreshed board.
- **The FAV chip and the fav/dog colouring are PINNED TO BET365, the same line the card quotes (founder ruling TEN-179, gate `f98f90cc`, 2026-09-10; shipped 2026-09-11).** *"A card can show — / — and still carry a FAV chip. That's incoherent. Pin both to the same Bet365 source as the price. If Bet365 has no price, no FAV chip and no colouring — dash means we don't know, and we shouldn't be asserting a favourite from a book we're not showing."* Both now derive from `_mcBet365Now(m)` via `hasFavLine` / `p1IsFavorite` in `renderMatches()` — **never from `m.odds`**, whose headline bookmaker is most often Pinnacle. This applies to **both** card states: an Upcoming row and a Completed row lose the chip alike when bet365 has no two-legged price. **`hasOdds` deliberately still reads `m.odds` and now feeds exactly one thing — `oddsSlot`** ("some book quotes this fixture, so keep the slot and dash it"): that is a layout decision, not a claim about a favourite, and collapsing it into the pin is a regression. **Measured before shipping (981 committed `matches.json` snapshots, 2026-07-08 → 09-10, 1,192 distinct fixtures, 73,053 card instances): a card GAINS a chip 0 times** — `m.odds` is present on ~96% of carded fixtures, so the "no `m.odds` but a bet365 price exists" case does not occur. That is what makes the pin safe for **TEN-182**: chips only ever disappear, so the name track only ever gains room. Measured on the rendered DOM at 1378px and 1400px: score-cluster x, divider x and row height are **identical** before and after (220/338/54 and 231/349/54); a chip-losing row's name box goes **107.8px → 147.38px** and `B. Van De Zandschulp` stops clipping (truncated rows 4 → 3). Tests: (a) a fixture whose only market is a non-bet365 book renders `— / —` **and no FAV chip** in both modes; (b) a bet365 price via any of the three `_mcBet365Now` sources (timeline / bet365-labelled `bestOdds` / bet365-headline `m.odds`) puts the chip on the **shorter** leg; (c) a one-legged or split-book bet365 line yields **no** chip; (d) a chip must never appear on a card that did not carry one before the pin. **Known and NOT fixed by this ruling — a level bet365 market (`p1 === p2`) still puts FAV on p2** (`!p1IsFavorite` is true when the prices are equal); measured at 172 upcoming / 336 completed card instances across the same census. Reported to the founder, awaiting his call; do not "tidy" it either way without a ruling. **The colouring half of this ruling is currently inert:** `p1Color`/`p2Color` are computed and rendered **nowhere** (`--mx-fav-odds` `#43d399` / `--mx-dog-odds` `#8a93a3` appear only in those two assignments), exactly like `bookmakerLine`. They are pinned so they are correct if ever restored — do not delete them, and do not wire them up without a design ruling.
- **Live feed — interruptions STAY, retirements clear fast (founder ruling 2026-09-03, TEN-107).** *"The interruptions we had due to rain — the matches should just stay there in the live feed and not disappear; and retirements, like the Moutet game, should disappear from the live feed — it took a couple of hours, it shouldn't take so long."* Two cases, opposite handling, and **never a wall-clock timeout** (a timeout would wrongly drop rain-suspended matches). **(1) Rain-suspended** (`event_status` ∈ `Interrupted`/`Suspended`) → the card **stays** on the board, `interrupted=true`, `live=false`, partial score refreshed; it is never dropped and never given a winner. **(2) Retirement / terminal** (`Retired`/`Walk Over`/`Finished`) → the card must clear within **one 10-min `refresh-scores.py` cycle**, not hours. Root cause of the lag was a timezone join miss: a board card's `date` comes from the odds feed's `commence_time` in **UTC**, while the api-tennis fixture's `event_date` is **account-local** — a late-evening match lands one calendar day apart, so the exact-key `(date, {p1Key,p2Key})` join missed and the terminal flag was never stamped. Fix: `refresh-scores.py` widens its fixture fetch by **±1 day** and, on an exact-key miss, falls back to the adjacent day for the **same unordered player pair** (preferring an actionable — finished/interrupted/live — fixture, tie-broken by start-time proximity). **Guard A:** a terminal `event_status` beats a lagging `event_live='1'` in **both** `refresh-scores.py` and `bsp-pipeline.js` (`match.live = event_live==='1' && !['Finished','Retired','Walk Over'].includes(event_status)`) so a decided match never renders as a phantom LIVE card. Regression test: `test_retirement_clear.py` (retirement joined across a ±1-day skew and cleared; rain-Suspended stays with `interrupted=true`; Guard A holds; no card silently dropped). **Duplicate guard:** the board's tour+surname dedup passes are defeated when the two feeds render a match differently (un-aliased tour label, initials-only names) — that is how a match survived as TWO cards (one stranded-live odds card, one completed api-tennis twin). `bsp-pipeline.js` runs a final `dedupeByPlayerKeyPair(matches)` pass keyed on the unordered **numeric api-tennis player-key pair** (identical across both feeds, unlike surnames) within a **2-day** window, keeping the richest card (finalScore > interrupted > live > has-odds > fixture-only); two distinct ATP singles matches cannot share a player pair within 2 days, so only true duplicates are removed (`test_dedup_guard.js`). Test: an `Interrupted`/`Suspended` fixture keeps its card with `interrupted=true`/`live=false`; a `Retired` fixture dated ±1 day off the card is stamped `retired=true`/`live=false` in one refresh; no elapsed-time cutoff drops any live/underway card; no two cards ever share the same `{p1Key,p2Key}` pair within a 2-day window.

---

## Non-negotiables — these are hard rules

- Never show a pipeline health banner or infrastructure warnings to end users
- Never highlight the better stat between two players with colour — neutral display only
- Never show "went the distance (4+ sets)" stat for best-of-three format tournaments
- Recent form calculations always include Challenger and ITF matches — never ATP-only
- All tournament records must reflect full career history, not a truncated date range
- Closing odds must be preserved through pipeline rebuilds — never recomputed at display time
- Opening AND closing odds for a completed match are ALWAYS the bet365 stream (founder ruling TEN-124, 2026-09-02: bet365 is the most trustable soft book). Both legs pin to bet365 — a single-book journey, never cross-book. If bet365 is absent for a completed match, dash the journey; never substitute another book.
- A closing odd requires a PROVEN pre-first-ball reference (real startTs, or an in-play onset detected off bet365's own tick burst). No proof → dash. Nothing after a match's original first ball counts (a suspension/restart must not move the open→close journey). Missing close is a dash, never a substitute or in-play price.
- The bet365 history archive stores **PRE-MATCH points ONLY — in-play is discarded, permanently** (founder ruling gate `d7a24b42`, 2026-09-10, TEN-179; he picked p1, "pre-match only, as shipped"). Measured on 51 real archived fixtures, **96.3% of the bet365 series is in-play** (428 pre-start points vs 11,257) — so this is the whole sizing question, not a detail: pre-match-only is ~5.4 MB raw / ~1.4 MB gzipped for the full window, and the 24-point reduction cap discards 0 of 428 pre-start points. It follows the TEN-124 rule above: the close is pinned pre-first-ball, so an in-play point can never be a displayed price. He ruled knowing it is **irreversible** — we never re-pull and oddspapi prunes density after ~2 weeks, so in-play not captured on the night is gone for good. Test: no shard under `bet365-history/` contains a point at or after its fixture's first ball; `archive-bet365-history.py verify` fails on an in-play close; re-opening this needs a new founder ruling, not a code change.
- The Clay surface tag must never render in green (it's orange/terracotta)
- Confidence percentage: no decimals (show 88%, not 88.5%), white colour not orange
- **`openingOdds.at` is an ODDSPAPI INGESTION INSTANT, not bet365's posting time — and the Upcoming NOW leg is a DIFFERENT BOOK from the OPEN leg (TEN-179, 2026-09-10; measured, ruling PENDING on gate `6c80b1f2`).** **(1) The open we store is the first price oddspapi observed, not the price bet365 opened at.** `bsp-pipeline.js:4923` sets `at` from the series' own first-tick `createdAt`, and those timestamps **CLUSTER**: across the 9 settled US Open fixtures, three share `2026-09-06T17:01:5x` (spread **2.2 seconds**), two share `2026-09-07T04:50:4x`, two share `2026-09-08T08:23:5x`. Bet365 does not open three different match-winner markets 2.2s apart — those are oddspapi ingest batches. **Do not quote the 27.7–46.2h "lead time" spread as when bet365 posts;** it is when oddspapi ingested. A **404 from `/v4/historical-odds` is an ANSWER (not ingested yet), not a transport failure** — verify with a positive control on a settled fixture in the same minute before reporting a fault, and check the production capture log first: it already asks these fixtures every run (`refresh-odds-history.py:532`, upcoming → always refresh). **(2) The cross-book gap.** `bsp-consult-dashboard.html:7749/7756` render the Upcoming drift as `mcDriftCell(m.openingOdds?.pN, m.bestOdds?.pN?.price ?? m.odds?.pN)` — OPEN is pinned bet365 (`bsp-pipeline.js:4918` nulls it otherwise) while NOW is **`bestOdds`, the highest price across ALL books**. Measured on the live board, best-across-books sits a mean **3.1%** and max **8.0%** above the reference book's own price, **always in the same direction**, which is enough to flip the sign of a small drift and reorder the Drift sort. The Completed leg is correct (`_mcCloseOf`, line 6504, TEN-124: pinned close, *never* a live `bestOdds`/`odds` fallback); the same discipline was simply never applied to Upcoming. **This is UNRESOLVED and must not be silently changed in either direction** until gate `6c80b1f2` is answered. Test: if NOW is ever pinned to bet365, `m.bestOdds` must not appear in the Upcoming `mcDriftCell` call sites; if it stays, the card must carry a book label. Related: TEN-180.

- Player names in match lists must never be truncated — use flex-grow layout. **EXCEPTION — the Completed card truncates, and that is the shipped state (founder ruling 2026-09-10, TEN-179, `c78b0e8`):** *"Names truncate with an ellipsis if they exceed it rather than pushing the scores right."* `.mc-players.cmpl .mc-row` column 1 is `minmax(0,260px)` — a track with **no content input** — and `.mc-players.cmpl .mc-name` is `nowrap` / `overflow:hidden` / `text-overflow:ellipsis`. This **reverses gate `439b9cbe` ("keep the wrap")**, which he superseded directly. The reason is structural and is why it must not be "fixed" back: each player row is its **own** grid, so any content-sized name track makes the two rows of one card start their scores at different x (measured on the live Mon 7 board: 36.1px apart on one card, 82px on another). A wrap cannot solve it either — it only adds a second line and still cannot move the score column. Flex-grow and the no-truncate rule still hold **everywhere else**, including the **Upcoming** card, which is untouched (`228px 130px 120px`, `nowrap`, no ellipsis). Test: on Completed, every player row's set cells share one card-relative x tuple regardless of name length (measured on live: `[195,233,257,281,305,329]` across 3-, 4- and 5-set cards on one board); on Upcoming, no `.mc-name` carries `text-overflow:ellipsis`.


- **Every published OPEN names its source; the ingestion point is PUBLISHED, not withheld (founder ruling TEN-179, 2026-09-11 — deliberately REVERSING his own 2026-09-10 gate).** *"If the only open we have is oddspapi's ingestion point, publish it rather than showing nothing… Where both are available, prefer the honest pin. Fall back to the ingestion point only when there's nothing better. Never derive, never interpolate, never take the series low. Report which of the two each published open came from."* He accepted the cost explicitly: on ~1.7% of fixtures we publish a price bet365 was not showing at that instant (Zverev 1.167 vs a real 1.22); on 98.3% the two values are identical, and an empty OPEN column costs more. **Preference order, unchanged and still correct: honest pin (`src:'first-sighting'`) → archive (`src:'archive'`, only when genuinely earlier) → our own ingestion point (`src:'ingestion'`).** The archive swap **stays gated off a `first-sighting` pin** — do not "ungate" it: the archive stores the same `/v4/historical-odds` series, so its first point is an ingestion instant too, and letting it win would replace the value the ruling prefers with the one it falls back to. **`src` is now mandatory on every published open.** It used to be absent on the fallback, which made "ingestion point" indistinguishable from "field not set" and the split unreportable off the board. Labelling happens in the provenance census over `matches` — the point where *published* is defined — **not** on the carry-forward path, because an open can reach the board with `m.openingOdds` already set and no `priorOdds` hit; that one published unlabelled. Spread, never mutate: one `priorOdds` record is shared by two map keys. An absent `src` is *provably* the ingestion point (every pre-ruling pin was `series[0]`), so labelling it is a statement of fact and **changes no value** — forward-only is preserved. **A premise about the board must be measured against the board before it is built on:** the OPEN was never actually being withheld (the Upcoming drift cell reads `m.openingOdds` directly and is *not* gated on `finalScore`), so the reported problem was the label, not the value. Tests (`tools/test-open-provenance.js`, 13 cases, run against the SHIPPED `bsp-pipeline.js` text): the honest pin beats an earlier archive open; an archive open replaces an ingestion pin only when strictly earlier; the pin is never the series minimum; a carried open keeps its value and gains `src:'ingestion'`; a shared `priorOdds` record is not mutated; the split line counts carried opens.


- **When oddspapi has no bet365 series, the OPEN falls back to the SAME BOOK read off api-tennis — write-once, vendor-tagged, PRE-MATCH ONLY, OPEN ANCHOR ONLY (founder ruling TEN-198, gate `05ded3ab`, 2026-09-13).** *"When oddspapi has no Bet365 price, take api-tennis's. Same book, second vendor… But it must be labelled… fallback applies to the **OPEN anchor only**. Never to NOW, never to drift, never to closing-line value."* Not a cross-book fallback and **not** a TEN-124 reopening: same book, second vendor, so no new source ruling was needed. Three decisions, all his: **capture = WRITE-ONCE** (keep the first bet365 sighting whenever it appears, never overwrite — measured **11/11** fixtures vs **5/11** for a build-time read; the build-time version would have missed the fixture that opened the issue, which api-tennis carried in **1 of 34** polls); **placement = INTO `m.openingOdds` with a `vendor` tag** (he chose this over a sibling field, **overruling the recommendation**, having been shown the consequence); **labelling = option A**, the existing provenance strip now reads `bet365 via api-tennis · Sep 12, 16:10 · seen by us`. **The placement choice is why the scope is NOT structural.** Every derived consumer must ask `_openPinIsVendor(m)` first — `bsp-consult-dashboard.html` routes all of them through **`_openDerivedOf`** (drift sort, `moveScore`, the day-summary "Biggest market move" panel, `mcOpenCoverage`'s denominator, `_mcOddsLanded`'s sort gate) and displays through **`_openAnchorOf`**; `mcDriftCell`/`mcJourney` take an `anchorOnly` flag that renders the OPEN price with **no bar and no delta**. Removing one guard without the others silently lets the fallback into a number the founder excluded. **PRE-MATCH ONLY is load-bearing and was nearly missed:** api-tennis keeps serving a fixture's Home/Away market *after* the match ends and `buildPastMatchObject()` calls `fetchApiTennisMatchOdds()` for **completed** fixtures, so an unguarded pin publishes a POST-MATCH price under the word "Opening" and write-once makes it permanent. `pinVendorOpen` therefore returns early on `m.finalScore` (same guard `m.bet365Now` already carries) and counts the rejection; a settled fixture's open can only arrive the honest way — captured while UPCOMING and carried forward by `priorOdds`. **What may and may not be claimed about the value:** 636 pre-match leg-observations across 9 fixtures — **98.7%** of api-tennis bet365 observations are the same bet365 price **TRUNCATED** to 2dp (`1.667 → 1.66`; **zero** counter-examples matched rounding), lagging a median **5.2h** / max **14.6h**. So it is a price bet365 genuinely showed (fine as a one-shot anchor), it is **never better than oddspapi's and up to 0.01 worse** (a systematic always-downward shade — the measured argument for keeping it out of CLV), and the only instant we can evidence is **when we looked**. Therefore the pin carries **`seenAt` and NO `at`** — manufacturing an `at` is exactly what the 2026-09-11 ruling threw out — plus `src:'vendor-sighting'` and `vendor:'api-tennis'`. The strip says **"seen by us"**, deliberately NOT "first sighting": the oddspapi pin prints bet365's own clock, this one prints ours. **Three traps that will silently undo this:** (1) `VENDOR_SRC` must stay in the provenance census's accepted-src set, or the census **rewrites** every vendor pin to `'ingestion'` on the next run and the tag becomes a lie one rebuild later; (2) the `!p1.length || !p2.length` dash branch must **exempt** a carried vendor pin — it nulls carried opens on the assumption they are cross-book, and these fixtures are by definition the ones with no oddspapi stream; (3) the archive swap must skip a vendor pin (`!m.openingOdds.vendor`), or an oddspapi **ingestion instant** replaces an evidenced sighting. **Do not gate any of this on api-tennis `event_status`:** the US Open final read `"1"`, not `"NS"`, 16 hours before first ball, and an `event_status==='NS'` pre-match filter silently dropped the single sighting this whole issue was about. **KNOWN GAP, measured, not yet ruled:** `fetchApiTennisMatchOdds()` is called only by `buildUpcomingMatchObject` (fixture-only) and `buildPastMatchObject`. **Odds-api-driven cards (bare-hex `id`, built by `buildMatchObject`) never call it and get no fallback.** Scale, re-derived over all 1,100 `matches.json` commits: hex cards are **34.0%** of card-instances (25,142/73,849), but **83.3% of them already carry an open**, so the population actually left uncovered is **5.7% of card-instances (4,200) / 3.4% of unique fixtures (42 of 1,225)**. They also carry no api-tennis event key, so closing it needs either a per-card `get_odds` or a new stashed key; tracked separately. Tests: `tools/test-open-provenance.js` (25 cases, run against the SHIPPED `bsp-pipeline.js` text) — both exit paths pin, oddspapi wins when both exist, write-once never overwrites, a completed fixture rejects a post-match sighting, an upcoming capture survives into the settled card, a carried vendor pin survives a bet365-less stream, the census does not relabel it, the archive does not overwrite it, a malformed sighting mints nothing, and the transient carrier never reaches `matches.json`.
- **NO RETROACTIVE BACKFILL — an OPEN may only ever be an observation our own live poll made (founder ruling TEN-198, gate `b2d536ed`, 2026-09-13).** Asked whether to import the one evidenced bet365 sighting for Zverev–Shelton — **1 of 42** collector polls, `2026-09-12T16:10:21.048Z`, 1.66 / 2.30, 25.8h before first ball, a comparable anchor to the two semis' 29.3h/33.3h — he chose **A, leave it dashed**: *"the fix is forward-looking and that is all it claims."* So the fixture that opened this issue **still shows a dash, by ruling**, and the shipped fallback covers only fixtures whose sighting a live poll of ours catches from now on. **The trap this closes:** the **11/11** coverage figure quoted in the gate above was measured by *replaying* the TEN-164 collector archive (`/Users/Michael/ten164-odds-probe/`, **outside the repo**) — it says what the code *would have* caught had it been running since 11 Sep, and it was not running. That replay is a **measurement artefact, not a feed**; wiring it in — or any hand-import of a past sighting — is the obvious "improvement" and is exactly what was ruled out. Re-measured at close-out (04:31Z, first ball 18:00Z): oddspapi `/v4/historical-odds` still **404 NOT_FOUND** on the final while the Tiafoe–Shelton semi returns **200 / 3.46 MB** on the same key and endpoint; api-tennis carries **7 books, no bet365**. Tests (`tools/test-open-provenance.js`, now **28** cases): a sighting no live poll of ours made mints nothing even when hung off a plausible backfill field; `fetchApiTennisMatchOdds()` is the **only** writer of the `apiTennisBet365` carrier (**exactly 2** write sites, each asserted to assign straight from a live call — a third is by definition a new provenance path and re-opens this gate); and the pipeline references no out-of-repo collector/replay artefact and the OPEN block reads no filesystem. **Each of the three was mutation-proven red** — a `|| m.collectorSighting` fallback, a third write site, and a `ten164-odds-probe` path each kill exactly one, and only one. **RESIDUAL EXPOSURE, reported and deliberately NOT guarded:** `priorOdds` is read from the committed `matches.json` (L4846) and carries `openingOdds` forward verbatim, so a **hand-edited open in that file would persist forever** under write-once. That route is closed by ruling and review, not by code; a checksum/provenance guard on carried opens would close it and has not been built because it was not asked for.
- **A leg guaranteed to cost nothing FAILS THE RUN when it spends (founder ruling TEN-179, 2026-09-11).** *"The zero-quota guarantee goes red. An annotation in a green run is exactly what nobody read for nine days. Any spend on a leg guaranteed to cost nothing fails the run loudly."* `refresh-odds-history.py --first-appearance` returns **`BILLED_RC = 9`** on any `/v4/account` meter delta, raises one ops alert, and `odds-capture-loop.sh` **stands the leg down for the rest of the window** and exits non-zero — on the normal exit **and on the SIGTERM trap**, which is the route every cancelled or timed-out job takes. A **distinct** code, not a generic 1: "bet365 has not posted a price yet" runs 96 times a day and must never redden a run (37 unread red runs, Sep 2-10, is the failure mode being avoided). **The check must be unconditional** — it originally sat below the early *"nothing to sweep"* return, so it was skipped on exactly the path where the only calls made are the two meter reads, and a quiet board takes that path every time; it now runs from `settle()` on every exit. Tests (`tools/test-first-appearance-billed.py`): meter unchanged → rc 0 and no alert; meter moved → rc 9 plus an `ops` alert. Shell contract, verified with a stubbed sweep: rc 9 → one sweep then stood down, **job exit 1**; rc 1 → retries, job exit 0; rc 0 → job exit 0; the metered leg fires once per hour in all three.

- **The open-lag TTL, the metered spend guard, the stale checkout and the widened sweep — RESOLVED (founder ruling TEN-179, 2026-09-11).** Supersedes the "UNRESOLVED, reported not implemented" entry this replaces.
  - **`MAP_MISS_TTL_H = 1.0`** (`refresh-odds.py`). *"One call per run rather than per fixture makes this cheap, and halving the worst-case open lag from 5h15m to 2h15m is worth 33.6% of quota."* The bound is **`TTL + metered interval + sweep interval`** — never quote the sweep interval as the open lag. `fixture_map()` makes at most **ONE** `/v4/fixtures` call per RUN covering the whole window, not one per missing fixture, so the ceiling is set by run count (hourly ⇒ ≤24/day); **a TTL below 1h is dead code** because the map has no writer other than the hourly `refresh-odds.py`. **The ceiling is not the measurement.** `odds-fixture-map.json → missAudit` records every run — spending or not — and `observed.ttlBitRuns` counts only the runs where an **expired MISS was the sole reason** a billable call was made; `projectedTtlUnitsPerDay` is the real figure to quote against the ≤24 ceiling, and `missResolutionHoursP50` is the benefit side. Test: a 61-min-old MISS is stale and a 59-min-old one is not; a run whose stale set contains an *unmapped* fixture does **not** count as the TTL biting.
  - **The metered leg is gated by a SPEND GUARD, not by `METERED_DUE`.** *"Spend guard, not a lock — covering the REDO re-arm, the spend at every loop restart, and the retry not being bill-aware."* `METERED_DUE` is set in four places and cleared in one, so the arming logic alone can never hold a flat hour. `metered-spend-guard.py` is the single place that answers *have we already paid for this hour* and it **defers, never cancels** — `METERED_DUE` stays armed. **Two stores because there are two resets:** an **untracked** `.metered-spend-marker` (survives `git reset --hard`, covers the REDO re-arm) and the committed `odds-quota-history.json` floor (survives a new runner, covers the restart); the floor is `max()` of the two. **The retry is bill-aware:** an attempt that measured a meter delta of **0** retries immediately (it cost nothing); an attempt still marked `attempted` — the process died before it could measure — counts as **BILLED**, because **a 400 bills**. `refresh-odds.py` arms the marker *before* the first billable call and settles it in a `finally`. **The guard fails OPEN on every unreadable store** — a guard that fails closed freezes the board over a bookkeeping error. Tests: billed 5 min ago → SKIP; 58 min ago → ALLOW; delta 0 → ALLOW; `attempted` → SKIP; an `odds-history`-tagged quota sample must **never** suppress the NOW leg (hence the `by` tag on `_record_sample`).
  - **A loop on a stale checkout stands down.** `actions/checkout@v4` runs once and the loop runs for up to 350 min, so **merging a fix does NOT put it in force** — and the versions skew: bash holds an open fd on the driver (so it runs its original bytes all window) while `python3 <file>` helpers are re-read every tick, and `ci-commit-push.sh` fetches only on a push rejection. A push race therefore yields **old driver + new Python**. `driver_moved()` compares the boot SHA against `origin/main` across the whole executable set and stands the loop down — but only after `request_successor()` has queued a replacement run (`actions: write`), because **a capture gap is worse than a stale capture** and Actions delivers ~27% of an hourly cron. Proven cost of not having this: `b25f180` merged the stale-NOW monitor 31 minutes after the loop started and it took **zero** measurements for the rest of the window, while its state file still read a healthy `n=1`. Test: boot SHA == `origin/main` → SAME; boot SHA before a driver commit → MOVED; boot SHA unknown → SAME (never stand down on a bad read).
  - **The free sweep covers ALL upcoming fixtures, not just unopened ones.** *"It's unmetered and its last tick is identical to the metered price, so this is free freshness."* This is what lets NOW move every 15 min at **zero quota** — measured 223 → 223 across a real widened sweep. **Three constraints that are not optional:** **(a) MERGE the series, never replace it** (`_merge_movement`) — oddspapi prunes historical density with age, the OPEN lives at the old end, and a wholesale replace would move the open to a later price, which is deriving an open from a newer quote; **(b) `firstSeenAt` may only be stamped for a fixture whose series THIS call created** (`open_monitor(..., newly_opened=)`) — it is set with `setdefault`, so re-sweeping a long-held fixture that has no monitor record would stamp `firstSeenAt = now` and `bsp-pipeline.js` would then resolve its OPEN to **the current price**; a fixture with no record keeps none and takes the explicit, counted `series[0]` fallback; **(c) the budget cut is never silent** — `SWEEP_BUDGET_S` (480s against a 900s tick, ~87 fixtures at `HIST_SLEEP` 5.5s) drops the most *distant* already-open fixtures and says how many, while every **unopened** fixture is swept regardless of budget because an unpinned OPEN is unrecoverable once the market moves. **Stale-NOW stays at 120 minutes** until publish lag has a real sample behind it — do not re-derive it off n=2.
  - **The free sweep must not roll NOW back.** `seed_from_live()` returns the **published** board and `write_matches()` writes it over the committed file, so the fresher locally-captured `bet365Now` was being replaced by the staler published copy every sweep — `oddsMovement` was carried forward, `bet365Now` never was. The carry-forward is **monotonic by construction**: it keeps the committed value only when its `observedAt` is strictly newer, so it can never replace a fresher read with a staler one, and completed matches are excluded (`bsp-pipeline.js` deletes `bet365Now` on a finished card). Test with a negative control — a fresher LIVE value must still win.
---

## Known bugs to fix (in priority order)

1. **H2H surface filter** — does not work correctly on the H2H page
2. **Weather integration** — `fetchMatchWeather` has never successfully run in production, needs live test
3. **Form bars** — confirmed to have had rendering issues on Today's Matches page (check current state before touching)
4. **Filter pills on Match Analysis modal** — should show tournament names with "All surfaces" dropdown, not surface type pills (Clay/Hard/Grass)

---

## How tasks arrive

Michael uploads task documents to Google Drive and shares them. Each document is a self-contained brief specifying:
- What to build or fix
- What not to touch
- Which data source to use
- Any feasibility checks required before writing UI

**Read the task document fully before writing any code. Complete any feasibility checks first and report back before implementing.**

---

## Agent roles

### Claude Code agent (developer)
You write and edit code. You do not design. When a task requires visual judgment, flag it rather than guessing. Your two core files are `bsp-pipeline.js` and `bsp-consult-dashboard.html` — treat them as a production system.

### Claude Design agent (visual/UI)
You produce design direction, annotated mockups, and UI specifications. You do not write implementation code. When Michael sends a task you will receive: a screenshot of the current state, a description of what needs to change, and sometimes a reference design. Your output must be specific enough for Claude Code to implement without guessing.

**Claude Design must never:**
- Suggest fabricated data or placeholder charts — flag data availability gaps instead
- Propose designs requiring new data sources not already confirmed available
- Redesign sections outside the stated task scope

---

## Context on BSP Consult as a business

- 400+ paying members
- Channels: TikTok, Instagram (~22K followers), Telegram, ClickFunnels email list, bspconsult.app
- Affiliate partnership: bet105 (crypto bookmaker)
- Analytical framework: W/UE ratios, first-serve %, surface-specific records, Grand Slam experience differentials, physical fatigue, altitude/court speed conditions
- Focus: ATP 250 and above only
- Preferred players (positive framing): Alcaraz, Musetti, Fils
- Less favourable framing: Zverev
- TikTok compliance: avoid raw sportsbook UI visuals and direct betting terminology in algorithmically surfaced content
